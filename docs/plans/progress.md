# Progress

Short orientation ledger only.  Detailed facts belong in subsystem docs;
shipped implementation history belongs in the history docs linked below.

## Current Status

**Active track**: supervised Access Router / Primary Agent runtime path is
landed; current work is release hardening and targeted per-subsystem
run-kernel E2E coverage.

**Runtime path**: `deepcli` launcher -> Supervisor -> Agent Hub + Access Router;
Agent Hub manages the Primary Agent Runtime process.  CLI, Probe, and future
frontends enter through ACP over Access Router WebSocket and route to Primary
Runtime.

**Kernel version**: `1.0.6` (session DB schema v1 pre-release baseline).

**Active code**:

- `src/kernel/` — Kernel, Supervisor, Access Agent, Agent Hub, Primary Runtime.
- `src/cli/` — thin TypeScript/Bun ACP client and TUI.
- `src/launcher/` — user-local launchers and release packaging.
- `src/probe/`, `scripts/` — ACP probe client, dev startup, release helpers.
- `archive/` — read-only daemon-era reference.

## Where History Lives

| Area | Durable record |
|---|---|
| Kernel architecture | [`../kernel/architecture.md`](../kernel/architecture.md) |
| Kernel shipped milestones | [`../kernel/history.md`](../kernel/history.md) |
| Kernel completed plans index | [`../kernel/history/plans/`](../kernel/history/plans/) |
| Kernel subsystem facts | [`../kernel/subsystems/`](../kernel/subsystems/) |
| CLI shipped milestones | [`../cli/history/implemented-summary.md`](../cli/history/implemented-summary.md) |
| CLI design facts | [`../cli/design.md`](../cli/design.md) |
| CLI completed plans | [`../cli/history/plans/`](../cli/history/plans/) |
| Launcher / installers | [`../launcher/history/plans/`](../launcher/history/plans/) |
| Future work | [`roadmap.md`](roadmap.md), [`backlog.md`](backlog.md) |

## Recent Log

Keep only the newest handful of entries here; move details to the owning
history or design document when this table grows.

| Date | Item | Result | Verification |
|---|---|---|---|
| 2026-05-24 | WebBridge WebFetch tool-path regression | Bumped Kernel/WebBridge extension version to `1.0.6`, fixed the `get_backend_by_name("browser")` missing import that caused `BrowserFetchBackend is not defined`, and added coverage that drives `WebFetchTool(... backend="browser")` through WebBridge to a fake extension response. Added a live-kernel E2E probe that pairs a fake extension, selects the browser backend, prompts the agent to use WebFetch, and asserts the fake extension receives `fetch_tab`. | `uv run pytest tests/kernel/tools/web/test_web_bridge.py -q` -> 4 passed; targeted `ruff check` passed; `python3 -m compileall src/kernel/kernel/agents/mustang/tools/web/fetch_backends src/kernel/kernel/__init__.py` passed; `cd src/web-bridge-extension && bun test` passed |
| 2026-05-24 | CLI keybinding hardening | Bumped `TUI_VERSION` to `1.0.5`, added selector-component escape/cancel regression coverage, changed `session/set_mode` mode sync to opt out of the generic 30s ACP timeout, fixed Access Router + Runtime websocket multiplexing so `session/set_mode` can complete while a prompt is waiting on `session/request_permission`, and made the CLI acknowledge mode switching without claiming the new permission mode before kernel ACK. | `uv run --project src/kernel pytest -q tests/e2e/test_access_router_local_path_e2e.py::test_access_router_set_mode_completes_while_prompt_waits_on_permission -m e2e` -> passed; `uv run --project src/kernel pytest -q tests/kernel/access_router tests/kernel/agents/mustang/runtime/test_runtime_entrypoint.py tests/kernel/session/test_session_manager.py tests/kernel/tool_authz/test_authorizer.py tests/e2e/test_access_router_local_path_e2e.py -m 'not slow'` -> 111 passed; `bun run tests/test_keybindings_escape_components.ts`, `test_session_resume_before_prompt.ts`, `test_cli_shortcuts_matrix.ts`, `test_input_controller_r4.ts`, `test_agent_session_adapter.ts`, `test_ui_golden_r5.ts` -> passed; targeted `ruff`, `compileall`, and `git diff --check` passed; `bunx tsc --noEmit --pretty false` still fails on documented pre-existing active-port type drift (`TruncationMeta`, status-line/session shim types, extension/MCP exports) |
| 2026-05-24 | WebBridge extension auto-pair | Bumped Kernel/WebBridge extension version to `1.0.5` and changed the Chrome extension to auto-discover `http://127.0.0.1:8200/web-bridge/status.json` after load/startup, store the current bridge URL/token, and connect without manual popup copy-paste. The popup remains as a manual override/repair path. | `python3 -m compileall src/kernel/kernel/agents/mustang/tools/web/web_bridge src/kernel/kernel/__init__.py` passed; `cd src/web-bridge-extension && bun test` passed; targeted `ruff check` passed; version assertion `kernel.__version__ == manifest.version` passed |
| 2026-05-24 | WebBridge extension version sync | Bumped Kernel version to `1.0.4` and synchronized the WebBridge Chrome extension package/manifest version to the same value so the installed browser card identifies the tested Kernel implementation version. | `python3 -m compileall src/kernel/kernel/__init__.py` passed; `cd src/web-bridge-extension && bun test` passed; targeted `ruff check` passed |
| 2026-05-24 | WebBridge install page chrome extensions link | Bumped Kernel version to `1.0.3` and changed the WebBridge installer step for `chrome://extensions` from copy-only to a primary hyperlink with copy fallback for Chrome builds that block `chrome://` navigation from HTTP pages. | `python3 -m compileall src/kernel/kernel/agents/mustang/tools/web/web_bridge src/kernel/kernel/__init__.py` passed; targeted `ruff check` passed |
| 2026-05-24 | WebBridge browser backend diagnostics | Bumped Kernel version to `1.0.2` and replaced the generic `Browser is not available. Check dependencies or API credentials.` backend-selection failure with WebBridge-specific pairing/offline guidance. | `python3 -m compileall src/kernel/kernel/agents/mustang/tools src/kernel/kernel/__init__.py` passed; targeted `ruff check` passed |
| 2026-05-24 | WebBridge browser WebFetch backend | Implemented the WebBridge runtime for explicit browser-backed WebFetch and bumped Kernel version to `1.0.1`: Primary Runtime starts a loopback WebSocket bridge, persists extension pairing secrets through SecretManager, exposes `_mustang.agent/web_bridge/*` ACP methods, surfaces `/webfetch browser install/status/pair/reset` in the CLI, serves a guided local install URL plus zip fallback, and adds a Chrome MV3 unpacked extension package under `src/web-bridge-extension/dist/chrome`. The `browser` backend is selectable but remains outside auto fallback. | `uv run pytest tests/kernel/tools/web/test_web_bridge.py -q` -> 3 passed; `uv run pytest -m e2e tests/e2e/test_web_bridge_install_page_e2e.py tests/e2e/test_web_fetch_backend_management_e2e.py -q` -> 2 passed; `cd src/web-bridge-extension && bun test` -> 1 passed; CLI targeted tests (`test_webfetch_service.ts`, `test_input_controller_r4.ts`, `test_autocomplete_sort.ts`) passed; targeted `ruff check`, `python3 -m compileall`, and `git diff --check` passed |
| 2026-05-24 | Slash command catalog closure | Closed the command-surface drift found after the `/skills` work. Kernel catalog, CLI slash execution, CLI autocomplete, Primary Runtime ACP dispatch, and real-kernel probe coverage now align for `/session resume`, `/cron list/create/delete`, `/memory list/show/delete`, `/global restore`, Agent lifecycle/grants, Gateway lifecycle/bindings, `/webfetch browser install/status/pair/reset`, `/skills`, and projected `/skill:<name>` commands. `/cost` now creates an empty session before fetching zero-token usage, matching the intended empty-CLI behavior. | `bun run tests/probe_real_kernel_slash_commands.ts` from `src/cli` -> `kernel_status_via_real_acp=true`, `skills_management_via_real_acp=true`, `skill_commands_via_real_cli_print=2`, `top_level_slash_commands_smoked=true`, `warnings=0`, `result=PASS`; `uv run --project src/kernel python tests/probe/probe_global_resource_store_final_e2e.py` -> `real_kernel_slash_commands=PASS`, `result=PASS`; `uv run --project src/kernel pytest -q tests/kernel/protocol/test_routing.py tests/kernel/commands/test_command_manager.py` -> 56 passed; runtime/access-router targeted pytest -> 16 passed; `bun run tests/test_agent_session_adapter.ts` -> PASS; `bun run tests/test_autocomplete_sort.ts` -> PASS; targeted `ruff check` passed |
| 2026-05-24 | CLI flag editor command cleanup | Moved TUI version diagnostics from the Welcome border into a right-column `Version` section, centered the Welcome border title, bumped `TUI_VERSION` to `1.0.3`, documented the TUI patch-version bump rule, and replaced the user-facing plural `/flags` command with singular `/flag`. `/flag` opens the editable selector, `/flag read <section>.<key>` reads one flag value, and `/flag set/reset <section>.<key>` stages restart-applied writes. | `bun run tests/test_ui_golden_r5.ts` -> 24 passed; `bun run tests/probe_global_resource_slash_commands.ts` -> `result=PASS`; `bun run tests/probe_real_kernel_slash_commands.ts` -> `result=PASS`; `uv run --project src/kernel pytest -q tests/kernel/commands/test_command_manager.py` -> 20 passed; `bun run tests/test_autocomplete_sort.ts` -> passed; `git diff --check` passed |
| 2026-05-24 | Real-kernel slash command coverage expansion | Expanded the real-kernel slash probe from fixed examples to a command-catalog-driven closure check. The probe now pulls the runtime `commands/list` catalog and activates every `source=skill` command through real CLI `--print /skill:<name>`, seeds fixtures for stateful `/secrets rename/delete`, exercises additional exposed subcommands (`/session info/switch/load`, `/model use`, `/gateways status`), and uses the CLI `ModelService` mapping so `/model list` is checked through the real client path. | `bun run tests/probe_real_kernel_slash_commands.ts` from `src/cli` -> `kernel_status_via_real_acp=true`, `skills_management_via_real_acp=true`, `skill_commands_via_real_cli_print=2`, `top_level_slash_commands_smoked=true`, `warnings=0`, `result=PASS`; `uv run --project src/kernel python tests/probe/probe_global_resource_store_final_e2e.py` -> `real_kernel_slash_commands=PASS`, `result=PASS` |
| 2026-05-23 | Command E2E proof audit and guardrails | Tightened slash-command E2E proof standards after runtime routing gaps were found. The real-kernel slash probe now imports the builtin command catalog and fails if any builtin slash command is not covered, configures a local fake OpenAI-compatible provider, verifies `/skill:skill-installer` through the real CLI `--print` activation path, and distinguishes the remaining mock slash probe as parser/dispatch-only. Also restored `/mcp` to the builtin slash autocomplete/catalog. | `bun run tests/probe_real_kernel_slash_commands.ts` from `src/cli` -> `kernel_status_via_real_acp=true`, `skills_management_via_real_acp=true`, `skill_command_via_real_cli_print=true`, `top_level_slash_commands_smoked=true`, `result=PASS`; `bunx tsc --noEmit --pretty false`; mock slash probe, autocomplete test, targeted kernel suite -> 104 passed; `git diff --check` passed |
| 2026-05-23 | Skill command activation ACP path | Fixed `/skill:<name>` execution on the active Access Router runtime path. The Primary Runtime direct ACP dispatcher now handles `_mustang.agent/session/activate_skill` instead of falling through to `tools_request`, so skill commands such as `/skill:skill-installer` reach `AgentSessionRuntimeService.activate_skill`. Added a runtime-entrypoint regression for the activate-skill method. | `uv run --project src/kernel pytest -q tests/kernel/agents/mustang/runtime/test_runtime_entrypoint.py tests/kernel/access_router/test_app.py tests/kernel/access_router/test_app_websocket.py` -> 16 passed; real-kernel slash smoke -> `result=PASS`; `/skills` regression suite -> 88 passed; targeted `ruff`, `mypy`, `tsc`, and `git diff --check` passed |
| 2026-05-23 | Real-kernel slash command probe | Split slash-command verification into a fast parser/dispatch mock probe and a real-kernel closure probe. The new probe starts `scripts/run-kernel.sh`, connects with the CLI `AcpClient`, invokes the real `executeBuiltinSlashCommand()` path, and smokes the current top-level slash command surface plus the primary management subcommands over Access Router `/session`. The run exposed and fixed `/flags set/reset` without explicit revision by reading the current section revision first. The final ResourceStore E2E aggregate now runs this real-kernel probe instead of relying only on the mock dispatch probe. | `bun run tests/probe_real_kernel_slash_commands.ts` from `src/cli` -> `kernel_status_via_real_acp=true`, `skills_management_via_real_acp=true`, `top_level_slash_commands_smoked=true`, `result=PASS`; mock slash probe passed; `bunx tsc --noEmit --pretty false`, targeted `ruff`, and `git diff --check` passed |
| 2026-05-23 | Access Router runtime-control ACP path | Fixed `/kernel status` and `/kernel restart` on the active Access Router `/session` path by handling `_mustang.agent/runtime/status` and `_mustang.agent/runtime/restart` locally through the Supervisor control socket instead of forwarding them to the Primary Runtime tool fallback. Added a websocket regression that unregisters the primary route after initialize and still proves runtime status/restart return from supervisor control. | `uv run --project src/kernel pytest -q tests/kernel/access_router/test_app.py tests/kernel/access_router/test_app_websocket.py tests/kernel/agents/mustang/runtime/test_runtime_entrypoint.py` -> 15 passed; `/skills` regression suite -> 88 passed; targeted `ruff`, `mypy`, and `probe_global_resource_slash_commands` passed |
| 2026-05-23 | Access Router runtime heartbeat | Added runtime-originated `_mustang.router/ping` notifications after Access Router registration and taught the router runtime websocket to consume those control messages during idle waits and in-flight ACP delivery. This keeps the primary route fresh across idle CLI periods, so the next runtime-routed command such as `/skills list` is not rejected as `route stale: primary`. | `uv run --project src/kernel pytest -q tests/kernel/access_router/test_app_websocket.py tests/kernel/access_router/test_router.py tests/kernel/agents/mustang/runtime/test_runtime_entrypoint.py` -> 21 passed; `/skills` regression suite -> 88 passed; targeted `ruff`, `mypy`, CLI autocomplete, and `probe_global_resource_slash_commands` passed |
| 2026-05-23 | `/skills` command and bundled skill installer | Added Kernel-owned `/skills` catalog surface, read-only skills ACP methods, bundled `skill-installer`, `skill:` canonical command projection with alias lookup, SkillManager record/inspect/refresh APIs, CLI `/skills` thin dispatch, and helper scripts for validate/install/provenance/check/update/audit/archive. Install/update/uninstall/search/sources route to `skill-installer` activation instead of a kernel write dispatcher. | `uv run pytest -q tests/kernel/protocol/test_routing.py tests/kernel/commands/test_command_manager.py tests/kernel/skills/test_skill_manager.py tests/kernel/skills/test_bundled.py tests/kernel/skills/test_eligibility.py` -> 88 passed; `uv run ruff check ...` -> passed; `uv run mypy ...` -> passed; `bun run src/cli/tests/probe_global_resource_slash_commands.ts` -> `skills_management_via_acp=true`, `skills_install_routes_to_skill_installer=true`, `result=PASS` |
| 2026-05-20 | Agent health heartbeat freshness | Hardened Access Router and AgentManager runtime health without reopening Global ResourceStore migration. Native WebSocket pong is not observable at the FastAPI/Starlette ASGI layer, so route freshness now means application-observable runtime transport activity: registration and successful runtime traffic refresh `last_seen`, WebSocket disconnect unregisters the route, stale routes report `stale` and reject delivery, and AgentManager health distinguishes deleted, dead process, unavailable route, stale route, and registered/fresh route. No ACP heartbeat was added, and `agent_bindings` remains unused. | `uv run --project src/kernel pytest tests/kernel/access_router tests/kernel/agent_hub -q` -> 55 passed; `uv run --project src/kernel pytest tests/kernel/protocol tests/kernel/commands -q` -> 152 passed; `uv run --project src/kernel python tests/probe/probe_agent_health_heartbeat_freshness.py` -> `native_pong_observable=false`, `stale_route_rejected=True`, `reconnect_restores_fresh=True`, `agent_bindings=0`, `result=PASS` |
| 2026-05-19 | Access Router management slash dispatch fix | Fixed `/agents`, `/gateways`, and `/mcp` ACP management requests on the real Access Router `/session` path. These ResourceStore management surfaces are now handled locally by Access Router instead of leaking into the Primary Runtime `agent.tools_request` fallback, which only supports runtime tool-management methods. Added a router WebSocket regression that proves the management requests do not reach Runtime. | `uv run --project src/kernel pytest tests/e2e/test_access_router_local_path_e2e.py::test_access_router_handles_agents_management_locally -q -m e2e` -> `1 passed`; `uv run --project src/kernel pytest tests/e2e/test_access_router_local_path_e2e.py tests/kernel/protocol/test_agent_management_routing.py -q` -> `5 passed, 4 deselected`; targeted `ruff`, `mypy`, and `git diff --check` passed |
| 2026-05-19 | Global ResourceStore SQLite final closure | Added a single final ResourceStore migration acceptance target that orchestrates the existing source-backed probes instead of duplicating their subsystem assertions. The final target covers aggregate ResourceStore closure, MCP ResourceStore + management surface, CLI slash management dispatch, and config/flags/secrets/schedule/agent/gateway/tool policy/skill/hook/memory/prompt ResourceStore paths. `agent_bindings` remains reserved/deferred by design. Agent health heartbeat freshness is now documented as an independent runtime follow-up, not a ResourceStore migration blocker. | `uv run --project src/kernel python tests/probe/probe_global_resource_store_final_e2e.py` -> `probe=global_resource_store_final_e2e`, `full_monolithic_e2e=True`, `agent_bindings=reserved_deferred`, `result=PASS`; `uv run --project src/kernel python tests/probe/probe_global_resource_store_closure.py` -> `result=PASS`; `uv run --project src/kernel python tests/probe/probe_mcp_resource_store.py` -> `mcp_management_surface=True`, `result=PASS`; `bun run tests/probe_global_resource_slash_commands.ts` from `src/cli` -> `mcp_management_via_acp=true`, `sqlite_direct_writes=0`, `result=PASS`; targeted kernel suite -> 1512 passed, 9 skipped, 24 deselected; targeted `ruff`, `mypy`, `tsc`, `git diff --check`, and `cloc` passed |
| 2026-05-18 | MCP management CLI/ACP surface | Added Kernel-owned MCP declaration management methods for list/read/create/update/delete, exposed `/mcp` in CommandManager, and wired CLI slash dispatch as a thin ACP client. The service mutates only ResourceStore-backed global MCP declaration rows, returns after-restart semantics, rejects plaintext credential-looking headers/env values, preserves `secret:<uuid>` refs, and leaves runtime MCP connection/session state out of durable declarations. Full monolithic E2E remains out of scope. | `uv run --project src/kernel python tests/probe/probe_mcp_resource_store.py` -> `mcp_management_surface=True`, `revision_after_add=2`, `revision_after_update=3`, `revision_after_delete=4`, `mcp_plaintext_leaked=False`, `management_response_plaintext_leaked=False`, `result=PASS`; `bun run tests/probe_global_resource_slash_commands.ts` from `src/cli` -> `mcp_commands=5`, `mcp_management_via_acp=true`, `sqlite_direct_writes=0`, `result=PASS`; `uv run --project src/kernel python tests/probe/probe_global_resource_store_closure.py` -> `result=PASS`; `uv run --project src/kernel pytest tests/kernel/mcp tests/kernel/config tests/kernel/protocol tests/kernel/commands -q` -> 269 passed; targeted `ruff check` and `mypy` passed; `bunx tsc --noEmit --pretty false` passed; `git diff --check` passed |
| 2026-05-18 | Prompt global declarations ResourceStore migration | Added `PromptDeclarationStore` over ResourceStore-backed `config_sections` for global prompt declaration metadata. Global user prompt ids, source paths, enabled state, and placeholder metadata import once from `<home>/prompts`; later filesystem manifest drift is reported and ignored while ResourceStore remains durable declaration truth. Prompt `.txt` bodies, rendered prompt text, session snapshots, and template render cache stay outside declaration rows. Project prompt overrides still win. MCP management CLI/ACP expansion and full monolithic E2E remain out of scope. | `uv run --project src/kernel python tests/probe/probe_prompt_resource_store.py` -> `probe=prompt_resource_store`, `prompt_startup_from_resource_store=True`, `legacy_drift_ignored=True`, `prompt_reload_sees_resource_store_update=True`, `project_override_wins=True`, `prompt_body_persisted_as_declaration=False`, `rendered_prompt_persisted=False`, `result=PASS`; `uv run --project src/kernel python tests/probe/probe_global_resource_store_closure.py` -> `result=PASS`; `uv run --project src/kernel pytest tests/kernel/prompts tests/kernel/config -q` -> 62 passed; targeted `ruff check` passed; targeted `mypy` passed; `cloc src/ --by-percent c` -> 23.67% comments; `git diff --check` passed |
| 2026-05-18 | Memory global declarations ResourceStore migration | Added `MemoryDeclarationStore` over ResourceStore-backed `config_sections` for global memory declaration/policy metadata. Legacy global `memory/config.md` disposition imports once into SQLite; later filesystem drift is reported and ignored while ResourceStore remains durable truth. Actual memory entries, Markdown bodies, embeddings/vector data, generated indexes, recall caches, session summaries, runtime notes, and logs stay outside declaration rows. Project memory entries still load from project scope. Prompt declarations remain out of scope. | `uv run --project src/kernel python tests/probe/probe_memory_resource_store.py` -> `probe=memory_resource_store`, `memory_startup_from_resource_store=True`, `legacy_drift_ignored=True`, `memory_reload_sees_resource_store_update=True`, `memory_entries_loaded_from_filesystem=True`, `project_scope_still_loaded=True`, `memory_data_persisted_as_declaration=False`, `plaintext_secret_leaked=False`, `result=PASS`; `uv run --project src/kernel python tests/probe/probe_global_resource_store_closure.py` -> `result=PASS`; `uv run --project src/kernel pytest tests/kernel/memory tests/kernel/config -q` -> 128 passed; targeted `ruff check` passed; targeted `mypy` passed; `cloc src/ --by-percent c` -> 23.67% comments; `git diff --check` passed |
| 2026-05-18 | Hook global declarations ResourceStore migration | Added `HookDeclarationStore` over ResourceStore-backed `config_sections` for global/user hook declaration metadata. User `HOOK.md` manifests and trigger bindings import once into SQLite; later filesystem manifest drift is reported and ignored while ResourceStore remains durable truth. Handler bodies, execution output, runtime queues, and generated artifacts stay outside SQLite. Project hook opt-in and CLI override layers still win. Memory / prompt declarations remain out of scope. | `uv run --project src/kernel python tests/probe/probe_hook_resource_store.py` -> `probe=hook_resource_store`, `hook_startup_from_resource_store=True`, `legacy_drift_ignored=True`, `hook_reload_sees_resource_store_update=True`, `fire_used_real_handler=True`, `project_override_wins=True`, `cli_override_wins=True`, `runtime_execution_state_persisted=False`, `handler_plaintext_leaked=False`, `result=PASS`; `uv run --project src/kernel python tests/probe/probe_global_resource_store_closure.py` -> `result=PASS`; `uv run --project src/kernel pytest tests/kernel/hooks tests/kernel/config -q` -> 94 passed; targeted `ruff check` passed; targeted `mypy` passed; `cloc src/ --by-percent c` -> 23.67% comments; `git diff --check` passed |
| 2026-05-18 | Skill global declarations ResourceStore migration | Added `SkillDeclarationStore` over ResourceStore-backed `config_sections` for global/user skill declaration metadata. User `SKILL.md` manifests import once into SQLite; later filesystem manifest drift is reported and ignored while ResourceStore remains durable truth. Skill bodies, supporting file contents, invocation cache, and generated runtime state stay outside SQLite. Project and CLI override layers still win. Hook / memory / prompt declarations remain out of scope. | `uv run --project src/kernel python tests/probe/probe_skill_resource_store.py` -> `probe=skill_resource_store`, `skill_startup_from_resource_store=True`, `legacy_drift_ignored=True`, `skill_reload_sees_resource_store_update=True`, `project_override_wins=True`, `cli_override_wins=True`, `skill_body_plaintext_leaked=False`, `secret_default_plaintext_leaked=False`, `result=PASS`; `uv run --project src/kernel python tests/probe/probe_global_resource_store_closure.py` -> `result=PASS`; `uv run --project src/kernel pytest tests/kernel/skills tests/kernel/config -q` -> 149 passed; targeted `ruff check` passed; targeted `mypy` passed; `cloc src/ --by-percent c` -> 23.68% comments; `git diff --check` passed |
| 2026-05-18 | Tool policy / tool-specific config ResourceStore revision checks | Confirmed `config.permissions` and `config.web_fetch` inherit ResourceStore durable truth through `ConfigManager(resource_home=...)` rather than needing a new store. Added revision exposure on config sections and made `RuleStore` reload parsed permissions when the bound section revision changes, so `ConfigManager.refresh_from_resource_store()` affects ToolAuthorizer before the next authorization call. WebFetch global config remains `config_sections`-backed, project/local/CLI overlays still win, and stored WebFetch API key refs now use stable `secret:<uuid>` refs instead of plaintext or legacy names. Runtime auth decisions and invocation logs remain non-durable runtime state. Skill / hook / memory / prompt declarations remain out of scope. | `uv run --project src/kernel python tests/probe/probe_tool_policy_resource_store.py` -> `probe=tool_policy_resource_store`, `tool_policy_startup_from_resource_store=True`, `web_fetch_startup_from_resource_store=True`, `policy_refresh_updates_before_call=True`, `web_fetch_refresh_updates_section=True`, `web_fetch_secret_ref_stable=True`, `tool_config_plaintext_leaked=False`, `result=PASS`; `uv run --project src/kernel python tests/probe/probe_global_resource_store_closure.py` -> `result=PASS`; `uv run --project src/kernel pytest tests/kernel/tool_authz tests/kernel/tools tests/kernel/config -q` -> 763 passed, 9 skipped, 24 deselected; targeted `ruff check` passed; targeted changed-module `mypy` passed; `cloc src/ --by-percent c` -> 23.69% comments; `git diff --check` passed |
| 2026-05-18 | MCP global declarations ResourceStore migration | Added an MCP-owned declaration facade over ResourceStore-backed `config_sections`, imported legacy `<home>/config/mcp.yaml` once during startup, and kept later YAML drift as report-only while ResourceStore remains durable truth. `MCPManager` startup now consumes the ResourceStore-backed `mcp.mcp` section through `ConfigManager`, with project/local/CLI layers still applied above global SQLite. Runtime connection state remains in-memory, and OAuth tokens remain secret refs rather than plaintext declaration rows. Tool policy / skill / hook / memory / prompt declarations remain out of scope. | `uv run --project src/kernel python tests/probe/probe_mcp_resource_store.py` -> `probe=mcp_resource_store`, `mcp_startup_from_resource_store=True`, `legacy_drift_ignored=True`, `oauth_secret_ref_preserved=True`, `mcp_plaintext_leaked=False`, `result=PASS`; `uv run --project src/kernel python tests/probe/probe_global_resource_store_closure.py` -> `result=PASS`; `uv run --project src/kernel pytest tests/kernel/mcp tests/kernel/config tests/kernel/storage -q` -> 142 passed; targeted `ruff check` passed; targeted `mypy` passed; `cloc src/ --by-percent c` -> 23.69% comments; `git diff --check` passed |
| 2026-05-18 | Gateway create/delete ACP surface | Added Kernel-owned `/gateways create` and `/gateways delete --confirm` ACP methods. Create declares a gateway in Access Router `access_adapters`; delete removes the gateway declaration, appends an `adapter.deleted` event, and disables related `access_channel_bindings` without touching agents, workspaces, or `agent_bindings`. CLI remains thin and now dispatches create/delete through ACP. | `uv run --project src/kernel python tests/probe/probe_agent_gateway_acp.py` -> `gateway_created=True`, `gateway_deleted=True`, `gateway_delete_disabled_bindings=True`, `agent_bindings=0`, `probe=agent_gateway_acp result=PASS`; `bun run tests/probe_global_resource_slash_commands.ts` from `src/cli` -> `gateway_create_delete_via_acp=true`, `sqlite_direct_writes=0`, `result=PASS`; `uv run --project src/kernel python tests/probe/probe_global_resource_store_closure.py` -> `result=PASS`; `uv run --project src/kernel pytest tests/kernel/config tests/kernel/flags tests/kernel/secrets tests/kernel/schedule tests/kernel/agent_hub tests/kernel/access_router tests/kernel/protocol -q` -> 370 passed; CLI targeted tests and `bunx tsc --noEmit --pretty false` passed; targeted `ruff check` and `mypy` passed |
| 2026-05-18 | CLI thin global-resource slash execution | Wired the active-port CLI slash-command registry to the existing Kernel ACP management methods for `/global`, `/flags`, `/secrets`, `/agents`, `/agent send`, and `/gateways` via `MustangSession.managementRequest()`. The CLI remains a parser/rendering layer and does not write SQLite. `/gateways create/delete` report unsupported because the current Kernel ACP surface exposes list/status/enable/disable/reload/bindings/bind/unbind, not create/delete. | `bun run tests/probe_global_resource_slash_commands.ts` from `src/cli` -> `probe=cli_global_resource_slash_commands`, `sqlite_direct_writes=0`, `result=PASS`; `uv run --project src/kernel python tests/probe/probe_global_resource_store_closure.py` -> `result=PASS`; CLI targeted tests passed (`test_input_controller_r4.ts`, `test_autocomplete_sort.ts`, `test_editor_slash_argument_autocomplete.ts`); `bunx tsc --noEmit --pretty false` passed; `uv run --project src/kernel pytest tests/kernel/config tests/kernel/flags tests/kernel/secrets tests/kernel/schedule tests/kernel/agent_hub tests/kernel/access_router tests/kernel/protocol -q` -> 369 passed; targeted `ruff check` and `mypy` passed |
| 2026-05-18 | Global ResourceStore aggregate closure probe | Restored source-backed aggregate closure evidence for the completed Global ResourceStore SQLite slices. Added a probe covering Config refresh, frozen Flag startup plus after-restart writes, UUID secret refs with no plaintext export, `/global`/`/flags`/`/secrets` ACP paths, `/agents`/`/gateways` shared `access_channel_bindings`, AccessRouter-backed `/agent send`, Agent delete cleanup, and ScheduleManager ResourceStore startup. This does not cover CLI thin slash execution, MCP/tool policy/skill/hook/memory/prompt declarations, or full monolithic E2E. | `uv run --project src/kernel python tests/probe/probe_global_resource_store_closure.py` -> `probe=global_resource_store_closure`, `agent_bindings=0`, `result=PASS`; `uv run --project src/kernel pytest tests/kernel/config tests/kernel/flags tests/kernel/secrets tests/kernel/schedule tests/kernel/agent_hub tests/kernel/access_router tests/kernel/protocol -q` -> 369 passed; targeted `ruff check` passed; targeted `mypy` passed; `cloc src/ --by-percent c` -> 23.71% comments; `git diff --check` passed |
| 2026-05-18 | ScheduleManager ResourceStore migration | Migrated `CronStore` / `ScheduleManager` durable task truth to ResourceStore `scheduled_tasks` rows with schedule events and revisions. Legacy `<home>/config/schedules.yaml` imports once; later YAML drift is reported and ignored. Agent delete now pauses owned schedules and clears `next_fire_at` through ResourceStore in the delete transaction. Runtime execution records remain process-local for this slice. `agent_bindings` remains unused. | `uv run --project src/kernel pytest tests/kernel/schedule tests/kernel/agent_hub tests/kernel/access_router tests/kernel/protocol -q` -> 279 passed; `uv run --project src/kernel pytest tests/kernel/tools/builtin/test_cmd_and_cron_tools.py -q` -> 9 passed; `uv run --project src/kernel python tests/probe/probe_schedule_resource_store.py` -> `probe=schedule_resource_store result=PASS`, `revision_after_add=1`, `revision_after_update=2`, `agent_delete_disabled_schedule=True`; targeted `ruff check` passed; targeted `mypy` passed; `cloc src/ --by-percent c` -> 23.71% comments; `git diff --check` passed |
| 2026-05-18 | Agent delete lifecycle transaction | Completed `AgentManager.delete()` transaction semantics: soft-delete rows, set `state_dir_deletion_status=pending`, disable Access Router `access_channel_bindings`, revoke management grants, bump directory revision, preserve workspace, then clean manager-owned state dirs after commit and mark `deleted`. Startup retries pending state-dir cleanup. `agent_bindings` remains unused. Schedule disable/transfer remains deferred because ScheduleManager is still outside ResourceStore in this phase. | `uv run --project src/kernel pytest tests/kernel/agent_hub tests/kernel/access_router tests/kernel/protocol -q` -> 176 passed; `uv run --project src/kernel python tests/probe/probe_agent_delete_lifecycle.py` -> `probe=agent_delete_lifecycle result=PASS`, `agent_bindings=0`; targeted `ruff check` passed; targeted `mypy` passed; `cloc src/ --by-percent c` -> 23.72% comments; `git diff --check` passed |
| 2026-05-18 | Agent/Gateway management ACP surface | Registered `/agents`, `/agent`, and `/gateways` in the Kernel CommandManager catalog and wired ACP methods for Agent CRUD/identity/bindings/lifecycle/health/grants, Access Router-backed `/agent send`, and Gateway list/status/enable/disable/reload/bindings/bind/unbind. `/agents bind` and `/gateways bind` both write only `access_channel_bindings`; `agent_bindings` remains reserved/deferred. CLI slash execution remains a thin-layer follow-up. | `uv run --project src/kernel pytest tests/kernel/agent_hub tests/kernel/access_router tests/kernel/commands tests/kernel/protocol -q` -> 189 passed; `uv run --project src/kernel python tests/probe/probe_agent_gateway_acp.py` -> `probe=agent_gateway_acp result=PASS`, `agent_bindings=0`; targeted `ruff check` passed; targeted `mypy` passed; `cloc src/ --by-percent c` -> 23.73% comments; `git diff --check` passed |
| 2026-05-18 | Global/Flags/Secrets management ACP surface | Registered `/global`, `/flags`, and `/secrets` in the Kernel CommandManager catalog and wired Kernel-owned ACP methods for global backup/export/import dry-run/unavailable apply, restart-applied flag writes through ResourceStore `flag_sections`, and UUID-backed SecretManager metadata operations without plaintext exposure. CLI slash execution remains a thin-layer follow-up; this slice validates the ACP/probe path only. | `uv run --project src/kernel pytest tests/kernel/protocol/test_global_flags_secrets_management.py tests/kernel/commands tests/kernel/storage tests/kernel/flags tests/kernel/secrets tests/kernel/config -q` -> 134 passed; `uv run --project src/kernel python tests/probe/probe_global_flags_secrets_acp.py` -> `probe=global_flags_secrets_acp result=PASS`; targeted `ruff check` passed; targeted `mypy` passed; `cloc src/ --by-percent c` -> 23.75% comments; `git diff --check` passed |
| 2026-05-18 | Production SecretManager SecretStore bridge | Replaced production `kernel.core.secrets.SecretManager` durable truth with UUID-backed `SecretStore` rows while preserving legacy name-based `get/set/list/resolve` and MCP OAuth APIs as compatibility lookups. Added legacy sqlite3 schema import into UUID rows, stable `secret:<uuid>` rename behavior, and ResourceStore plaintext-export guard coverage. Command/ACP surfaces remain deferred. | `uv run --project src/kernel pytest tests/kernel/secrets tests/kernel/config -q`; WebFetch/MCP OAuth focused compatibility tests `8 passed`; `uv run --project src/kernel ruff check src/kernel/kernel/core/secrets tests/kernel/secrets/test_secret_manager.py`; targeted real-manager probe `probe=secret_uuid_reference result=PASS` |
| 2026-05-18 | Config/Flag ResourceStore startup slice | Wired `ConfigManager(resource_home=...)` and `FlagManager(resource_home=...)` to import legacy `kernel.yaml` / `flags.yaml` once, read global truth from ResourceStore, keep FlagManager startup-frozen, and refresh materialized config sections from SQLite revisions. Command surfaces and remaining global declarations stay open. | `uv run --project src/kernel pytest tests/kernel/config/test_sqlite_backend.py tests/kernel/flags/test_sqlite_backend.py tests/kernel/storage/test_legacy_import.py -q`; `uv run --project src/kernel pytest tests/kernel/config tests/kernel/flags -q`; `uv run --project src/kernel ruff check src/kernel/kernel/core/config src/kernel/kernel/core/flags src/kernel/kernel/agents/access/app.py src/kernel/kernel/agents/mustang/runtime/session_service.py src/kernel/kernel/agents/mustang/runtime/__main__.py`; targeted real-manager probe `probe=config_flags_resource_store result=PASS` |
| 2026-05-18 | CLI shortcut raw-key parity | Fixed fallback terminal key parsing for legacy Alt, CSI-u, and modified cursor sequences; added raw shortcut matrix coverage; fixed `!` / `$` execution before the first chat prompt so it creates/reuses a pending ACP session instead of rendering a local error. | `bun run src/cli/tests/test_tui_key_sequences.ts`; `bun run src/cli/tests/test_cli_shortcuts_matrix.ts`; `bun run src/cli/tests/test_input_controller_r4.ts`; `bun run src/cli/tests/probe_phase_b_pty.ts`; `bun run tests/probe_oobe_pty.ts` from `src/cli`; `bun run src/cli/tests/run_phase_b.ts`; `bun run src/cli/tests/run_phase_c.ts`; `bun run src/cli/tests/run_phase_d.ts`; `bunx tsc --noEmit --pretty false`; `git diff --check` |
| 2026-05-18 | run-kernel targeted E2E coverage loop | Reworked the current E2E loop to use `scripts/run-kernel.sh` as the real startup path, fixed ProbeClient initialize capability preservation, aligned Supervisor tests to the Hub + Access Router direct-child topology, restored ResourceStore-backed AgentManager API coverage, and verified every `tests/e2e/test_*_e2e.py` file by targeted file/batch runs rather than monolithic full E2E. | Targeted E2E batches: `test_kernel_e2e.py` 16 passed; secret/cost/session load/resume 11 passed; media/MCP 7 passed, 2 skipped; skill files 17 passed; runtime/topology/phase2 40 passed; prompt override 2 passed; git/hooks/repl/web 13 passed; agent tool 2 passed; ask-user 3 passed; plan-mode split 4 passed; task-background split 3 passed; converted tool/memory/schedule/monitor/send-message batches 28 passed. Targeted unit: 122 passed. Static: ruff passed; targeted mypy passed; bandit passed; `git diff --check` passed. |
| 2026-05-11 | WebFetch backend management | Added Config-backed WebFetch backend selection, Crawl4AI backend support, internal `/webfetch backend` and `/webfetch config` CLI flows, ACP methods, and removed Playwright/Readability as user-selectable backends. Follow-up centralized Hub runtime contract allowlist and added tests that scan Access/Runtime `agent.*` contracts so real CLI router forwarding cannot drift silently again. API-key backends now request credentials, validate new and existing keys, store valid keys in SecretManager, and switch only after validation passes. | `uv run --project src/kernel pytest tests/kernel/agent_hub/test_agent_hub_transport_c.py tests/kernel/tools/test_manager.py::test_web_fetch_external_backend_prompts_then_stores_valid_api_key tests/kernel/tools/test_manager.py::test_web_fetch_external_backend_validates_existing_api_key_before_switch tests/kernel/tools/web/test_fetch_fallback.py tests/kernel/tools/builtin/test_web_fetch_secondary.py`; `uv run --project src/kernel pytest -m e2e tests/e2e/test_web_fetch_backend_management_e2e.py`; `bun run tests/test_webfetch_service.ts && bun run tests/test_input_controller_r4.ts` |
| 2026-05-10 | Kernel Agent layout refactor | Reorganized active Kernel modules into `core`, `supervisor`, `agent_hub`, `agents/access`, and `agents/mustang`; moved Agent Hub contracts under Hub; renamed resource revision tracking; updated docs and probes for the new Agent/Mustang naming. | `uv run pytest tests/kernel -q`; targeted Phase2 lifecycle and cancel E2E probes; import smoke; `git diff --check` |
| 2026-05-09 | POSIX macOS installer path | Merged latest `origin/main`, added macOS amd64 / arm64 one-line release packaging, and collapsed Linux/macOS packaging into shared `packaging/posix` build/install implementations with thin platform wrappers. The published POSIX `install.sh` auto-detects Linux vs macOS; `install-macos.sh` remains a compatibility alias. | `bash -n src/launcher/bin/deepcli src/launcher/packaging/linux/*.sh src/launcher/packaging/macos/*.sh src/launcher/packaging/posix/*.sh install-dev.sh`; `sh -n src/launcher/packaging/linux/install.sh src/launcher/packaging/macos/install.sh src/launcher/packaging/posix/install.sh`; Ruby YAML parse for `.github/workflows/release-packages.yml`; macOS release-shaped installer smoke with temp HOME and fake private `uv` -> `installer-smoke-ok`; macOS build-script smoke with fake `uv` / `bun` for `arm64` and `amd64` -> `build-smoke-ok`; `git diff --check` |
| 2026-05-09 | REPL non-Windows completion | Retired the old JSON batch dispatcher and tests, moved primitive allowlist to `tools/repl/primitives.py`, and reduced the REPL plan to a completed record with only Windows probe remaining. | REPL/tool focused tests `38 passed`; REPL E2E `3 passed`; `git diff --check` |
| 2026-05-09 | Remove `/auth` slash command | Removed `auth` from the CommandManager catalog and gateway special-case handling; credential storage remains available through ACP `secrets/auth`. | `73 passed`; `git diff --check` |
| 2026-05-08 | Config-folder path cleanup | Moved canonical flags path to `~/.deepcli/config/flags.yaml`, kept legacy root flags as read-only fallback, confirmed CLI `client.yaml` stays under `config/`, and refreshed current docs/tests. | `18 passed`; CLI config/OOBE probes; REPL E2E `3 passed`; `git diff --check` |
| 2026-05-08 | CLI Welcome recent sessions sync | Welcome Recent sessions now refreshes from the ACP-backed session list after session lifecycle and prompt actions, with the active empty session merged locally so `/session new` is visible before the first persisted user turn. | `bun run src/cli/tests/test_agent_session_adapter.ts`; `bun run src/cli/tests/test_session_startup.ts`; `bun run src/cli/tests/test_input_controller_r4.ts`; `bun run src/cli/tests/test_session_list_mapper.ts`; `bun run src/cli/tests/test_autocomplete_sort.ts`; `bun run src/cli/tests/test_ui_golden_r5.ts`; `bun run src/cli/tests/test_session_selector_omp.ts`; `bun run src/cli/tests/test_session_picker.ts`; `bun run src/cli/tests/test_status_line.ts`; `bunx tsc --noEmit --pretty false` from `src/cli`; `git diff --check` |
| 2026-05-08 | Kernel-owned context usage snapshot | `usage_update` now carries per-session context `used` / `size`; `session/load` and `session/resume` rehydrate that snapshot; CLI status line and idle compaction read it instead of local transcript/cumulative token guesses. | `bun test ./src/cli/tests/test_status_line.ts ./src/cli/tests/test_agent_session_adapter.ts ./src/cli/tests/test_session_startup.ts ./src/cli/tests/test_session_resume_before_prompt.ts`; `uv run pytest tests/kernel/session/test_session_manager.py::test_resume_session_replays_usage_snapshot tests/kernel/session/test_session_manager.py::test_prompt_broadcasts_usage_update tests/kernel/session/test_client_stream_replay.py::test_replay_turn_completed_emits_usage_update tests/kernel/protocol/test_codec.py -q`; `uv run pytest tests/e2e/test_session_load_cost_replay_e2e.py -q -m e2e`; `uv run ruff format --check ...`; `uv run ruff check ...`; `bunx tsc --noEmit --pretty false`; `git diff --check -- ...` |
| 2026-05-08 | Scriptable REPL Tool v1 | Added out-of-process Python REPL worker, ToolExecutor nested dispatch, primitive tool wrappers, cwd propagation, prompt/docs updates, and `.deepcli` E2E sandbox fix. | `57 passed`; `3 passed in 70.20s` for `tests/e2e/test_repl_e2e.py`; `git diff --check` |
| 2026-05-08 | REPL rewrite plan audit | Rewrote `repl-rewrite.md` in Chinese as an implementable plan: verified Claude Code visible REPL gating, corrected DeepCLI executor/session assumptions, made ToolExecutor nested dispatch first, and added Windows shell rules. | `git diff --check -- docs/plans/repl-rewrite.md docs/plans/backlog.md`; plan link checker |

## Capability Snapshot

### Kernel / Runtime

- Supervisor-owned process lifecycle for Hub, Access, and Primary Runtime.
- ACP WebSocket transport through Access Agent with Hub router backend.
- SQLite-backed sessions, reconnect-safe `clientTurnId`, permission tunneling,
  mode/config updates, replay, and user REPL execution.
- LLM provider routing, tool authorization, built-in tools, MCP, skills,
  hooks, memory, schedule, git context, and commands.

### CLI / Launcher

- Thin ACP client with active-port TUI, permission dialogs, session/model/theme
  commands, reconnect handling, slash command catalog, and PTY probes.
- User-local `deepcli` launcher for release-shaped runtime startup, status,
  logs, restart/stop, and uninstall.
- Linux, macOS, and Windows installer paths with private DeepCLI `uv` / managed Python
  runtime and precompiled CLI artifact.

## Update Rules

- Add only the newest completion to **Recent Log**.
- Keep this file below roughly 80 lines; compress or move older rows when needed.
- Put durable facts in the owning docs:
  `docs/kernel/architecture.md`, `docs/kernel/subsystems/*`, `docs/cli/*`,
  `docs/launcher/*`, or completed-plan archives.
- Put gotchas and environment quirks in [`../lessons-learned.md`](../lessons-learned.md).
