# Progress

This file is the short status ledger.  Keep it useful for orientation:
current state, recent completions, and pointers to the durable CLI/kernel
history docs.  Detailed design facts belong in subsystem docs, CLI docs, or
plan files.

## Current Status

**Active track**: Agent Control Plane landed single-Primary architecture; hardening
and full-system regression coverage.

**Last landed implementation**: Linux amd64 one-line installer path — release
builds now produce a tarball containing Kernel source runtime, staged runtime
lock, precompiled CLI, launcher, and assets; installer deploys it with
DeepCLI-private `uv` and managed Python, then the installed launcher can start
the packaged Supervisor.

**Kernel version**: 1.0.0 (session DB schema v1 pre-release baseline).

**Active code**:

- Kernel: `src/kernel/`
- CLI: `src/cli/`
- Probe client: `src/probe/`
- Dev startup scripts: `scripts/`
- Archive: `archive/` is read-only reference material.

## Where History Lives

| Area | Durable record |
|---|---|
| Kernel shipped milestones | [`../kernel/history.md`](../kernel/history.md) |
| Kernel subsystem behavior | [`../kernel/architecture.md`](../kernel/architecture.md) and [`../kernel/subsystems/`](../kernel/subsystems/) |
| CLI shipped milestones | [`../cli/history/implemented-summary.md`](../cli/history/implemented-summary.md) |
| CLI design facts | [`../cli/design.md`](../cli/design.md) |
| ACP / ACPX alignment | [`acp-acpx-schema-alignment-plan.md`](acp-acpx-schema-alignment-plan.md) |
| Agent Control Plane | [`agent-control-plane.md`](agent-control-plane.md) |
| Future work | [`roadmap.md`](roadmap.md), [`backlog.md`](backlog.md) |

## Recent Log

| Date | Item | Result | Verification |
|---|---|---|---|
| 2026-05-08 | Release CI Node 24 opt-in | Updated the Linux release workflow to set `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24=true`, opting into GitHub's upcoming Node 24 action runtime now instead of waiting for the June 2026 default switch. | YAML parse via Python; `git diff --check -- .github/workflows/release-linux.yml docs/plans/progress.md` |
| 2026-05-08 | Bootstrap tag fix command | Added `scripts/release.sh fix` for failed bootstrap releases: it runs on `main`, requires a clean tree and final Kernel semver, checks CLI version projections, confirms the matching remote tag already exists, then pushes `main`, deletes the local / remote tag, recreates it at current HEAD, and pushes it again. Documented that this is only for bootstrap or unreleased failed assets, not for already-public stable releases. | `bash -n scripts/release.sh`; `scripts/release.sh --help`; `scripts/release.sh read-version` -> `1.0.0`; `scripts/release.sh check-version` -> `version-ok 1.0.0`; `git diff --check -- scripts/release.sh docs/workflow/release-tool.md docs/plans/progress.md` |
| 2026-05-08 | Release tag command | Added `scripts/release.sh tag` for the first bootstrap release and tag-only final release cases: it runs on `main`, requires a clean tree and final Kernel semver, checks CLI version projections, ensures the tag does not already exist locally/remotely, then pushes `main`, creates `v<version>`, and pushes the tag. | `bash -n scripts/release.sh`; `scripts/release.sh read-version` -> `1.0.0`; `scripts/release.sh check-version` -> `version-ok 1.0.0`; `git diff --check -- scripts/release.sh docs/workflow/release-tool.md docs/plans/linux-one-line-installer-plan.md docs/plans/progress.md` |
| 2026-05-08 | Release tool full workflow docs | Expanded the release tool documentation with an end-to-end workflow showing how feature work enters `dev`, how a release moves from `dev` to fixed `freeze`, how freeze fixes and prerelease bumps are handled, how `freeze` promotes to `main`, and which later section to use for each step. | `git diff --check -- docs/workflow/release-tool.md docs/plans/progress.md` |
| 2026-05-08 | Release tool docs | Added Chinese usage documentation for `scripts/release.sh`, covering branch prerequisites, `freeze`, `release`, version projection checks, tag ordering, safety switches, and failure modes. | `git diff --check -- docs/workflow/release-tool.md docs/plans/linux-one-line-installer-plan.md docs/plans/progress.md` |
| 2026-05-08 | Single-entry release script | Added `scripts/release.sh` as the branch/version helper: `freeze` reads the current Kernel version, asks for a final version if omitted, bumps to `<version>a1`, resets/pushes the fixed freeze branch from `dev`; `release` runs from `freeze`, strips `aN`/`bN`/`rcN`, fast-forwards and pushes `main`, then tags and pushes the tag. Synced CLI version projections to the Kernel `1.0.0` source of truth. | `bash -n scripts/release.sh`; `scripts/release.sh read-version` -> `1.0.0`; `scripts/release.sh check-version` -> `version-ok 1.0.0`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `cd src/cli && bun run tests/test_ui_golden_r5.ts`. |
| 2026-05-08 | Release branch/version strategy plan | Extended the Linux installer plan with the branch model for future releases: daily work targets `dev`, prerelease hardening happens on the fixed `freeze` branch with `aN`/`bN`/`rcN` Kernel-sourced versions, and `main` only receives final versions that trigger stable release assets. | `git diff --check -- docs/plans/linux-one-line-installer-plan.md docs/plans/progress.md` |
| 2026-05-08 | Linux amd64 one-line installer implementation | Reworked Linux release packaging from wheel-per-component artifacts to one `deepcli-linux-amd64.tar.gz` containing Kernel source runtime, release-generated `uv.lock`, precompiled Bun CLI executable, Bash launcher, and assets. Installer now downloads/copies the tarball, verifies checksums, installs a DeepCLI-private `uv`, uses it to install managed Python, creates a release-local Kernel venv with `uv sync --locked --no-dev`, and links `~/.local/bin/deepcli` to the release launcher. Added a GitHub tag release workflow for Linux amd64 artifacts and updated launcher docs to remove stale wheel assumptions. | `bash -n src/launcher/bin/deepcli src/launcher/packaging/linux/build-release.sh src/launcher/packaging/linux/install.sh src/launcher/packaging/linux/install-dev.sh install-dev.sh`; `DEEPCLI_RELEASE_DIR=/tmp/deepcli-release-smoke DEEPCLI_VERSION=1.0.0 src/launcher/packaging/linux/build-release.sh` -> wrote `deepcli-linux-amd64.tar.gz`, `install.sh`, `manifest.json`, `checksums.txt`; isolated install `HOME=/tmp/deepcli-install-smoke-home3 DEEPCLI_LOCAL_DIR=/tmp/deepcli-release-smoke DEEPCLI_LOCAL_UV=$(command -v uv) src/launcher/packaging/linux/install.sh` -> checksum OK, managed Python `3.13.11`, 42 runtime packages installed; installed launcher `deepcli kernel start --port 18478` -> success; `curl http://127.0.0.1:18478/access/readiness` -> `process_ready=true`, `hub_ready=true`, `primary_registered=true`, `default_route_ready=true`; `deepcli stop` -> `runtime: stopped`; `uv run pytest tests/kernel/test_entrypoints.py -q` -> `8 passed`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `git diff --check`. |
| 2026-05-07 | Linux one-line installer plan | Added a review draft for the Linux installer path: release tarballs contain Kernel source runtime + private venv setup, precompiled CLI binary, launcher, assets, and a private DeepCLI-owned `uv` binary that does not touch the user's PATH or global environment. | `git diff --check -- docs/plans/linux-one-line-installer-plan.md docs/plans/progress.md` |
| 2026-05-07 | Skill deletion prompt/runtime consistency | SkillRegistry now prunes file-backed skills when their `SKILL.md` files disappear, SkillManager clears invoked/deferred state and emits `skills_changed`, PromptBuilder injects an explicit empty Available skills reminder when no skills are currently callable, and the Skill tool instructions only trust the current turn's Available skills reminder. | `cd src/kernel && uv run pytest ../../tests/kernel/orchestrator/test_first_principles_prompt.py ../../tests/kernel/skills/test_skill_manager.py ../../tests/kernel/skills/test_registry.py ../../tests/kernel/commands/test_command_manager.py -q` -> `41 passed`; targeted `ruff format --check`; targeted `ruff check`; targeted `mypy`; live Supervisor control restart `2555967 -> 2561549`; live Probe `_mustang.agent/commands/list` -> `skillCommandCount=0`; live Probe resumed stale session `dbd05ed7-6765-4fda-abf7-4e937532196b`, asked current Skills -> answer said no callable skills, `permissionRequests=[]`, `stop=end_turn`. |
| 2026-05-07 | Resume mode restoration fix | Fixed Kernel session resume after Primary restart so the persisted permission mode is restored into the newly constructed Orchestrator, not only into `Session.mode_id`; this keeps CLI-displayed `Bypass` aligned with actual ToolAuthorizer behavior. | `cd src/kernel && uv run pytest ../../tests/kernel/session/test_session_manager.py::test_resume_session_restores_mode_into_new_orchestrator ../../tests/kernel/session/test_session_manager.py::test_set_mode_updates_config_snapshot ../../tests/kernel/session/test_session_manager.py::test_resume_session_binds_without_replay -q` -> `3 passed`; targeted `ruff`; targeted `mypy`; live ACP probe created session `4dd740a1-6dc3-48d7-96a9-8496c7ed432e`, set mode `bypass`, restarted Primary `2541154 -> 2541879`, resumed and prompted Bash `printf BYPASS_OK` -> `permissionRequests=[]`, Bash completed, `stop=end_turn`, output `BYPASS_OK`; `git diff --check`. |
| 2026-05-07 | Provider tool-call protocol recovery | Conversation history repair now scans the full retained history for assistant tool calls that are not immediately followed by matching tool results, inserts missing synthetic error tool results at the correct location, removes duplicate/orphan tool results, persists the repaired history as a snapshot, and retries once when an OpenAI-compatible provider rejects tool-call adjacency. | `cd src/kernel && uv run pytest ../../tests/kernel/orchestrator/test_history.py ../../tests/kernel/orchestrator/test_orchestrator.py ../../tests/kernel/llm_provider/test_openai_compatible_provider.py -q` -> `83 passed`; targeted `ruff`; targeted `mypy`; live ACP resume of session `dbd05ed7-6765-4fda-abf7-4e937532196b` returned `stop=end_turn`; post-run DB/OpenAI adjacency probe -> `violationCount=0`; `git diff --check`. |
| 2026-05-07 | Supervisor-controlled restart implementation | Implemented Supervisor-owned runtime lifecycle control: child crash monitoring with dependency-aware restart and degraded restart budget, private Supervisor control socket, runtime status/restart ACP operator methods, `/kernel status|restart` CLI handling, model-visible `RestartSelf` for controlled Agent self-restart after turn flush, and ToolAuthorizer hard-deny protection for raw runtime kill attempts even in bypass mode. | `cd src/kernel && uv run pytest ../../tests/kernel/supervisor/test_supervisor_c1.py ../../tests/kernel/tool_authz/test_authorizer.py ../../tests/kernel/tools/test_manager.py ../../tests/kernel/tools/builtin/test_restart_self.py ../../tests/kernel/commands/test_command_manager.py ../../tests/kernel/protocol/test_routing.py ../../tests/kernel/protocol/test_session_handler.py -q` -> `127 passed`; targeted `ruff`; targeted `mypy`; `cd src/cli && bun run tests/test_input_controller_r4.ts && bun run tests/test_agent_session_adapter.ts && bun x tsc -p tsconfig.json --noEmit`; live Supervisor probe killed Primary pid `2396872` and observed replacement pid `2396927` with runtime `status=ready`, `restartCounts={"primary":1}`; `git diff --check`. |
| 2026-05-07 | Supervisor-controlled restart plan | Added the implementation plan for making Supervisor the sole runtime lifecycle owner: dependency-aware child restart, Supervisor control socket, ACP operator restart/status methods, launcher restart migration, narrow Agent `RestartSelf`, and hard-deny runtime process protection for raw shell kills. | `git diff --check -- docs/plans/supervisor-controlled-restart-plan.md docs/plans/progress.md` |
| 2026-05-07 | Orphan tool-call history sealing | New user turns now seal any pending assistant `tool_calls` with synthetic error `tool_result` messages before appending the user's prompt, so resumed/interrupted sessions keep OpenAI-compatible history valid instead of sending a provider-invalid transcript. OpenAI-compatible HTTP errors now extract the upstream JSON `error.message` for clearer diagnostics, and the CLI no longer adds an `Unknown error` fallback when the streamed assistant text already contains the error. | `cd src/kernel && uv run pytest ../../tests/kernel/orchestrator/test_history.py ../../tests/kernel/orchestrator/test_orchestrator.py ../../tests/kernel/llm_provider/test_openai_compatible_provider.py ../../tests/kernel/llm_provider/test_format_openai.py -q` -> `97 passed`; targeted `ruff`; targeted `mypy`; `cd src/cli && bun run tests/test_ui_golden_r5.ts`; `cd src/cli && bun run tests/test_agent_session_adapter.ts`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `git diff --check`. |
| 2026-05-07 | File tool contract alignment | Aligned Kernel file tools with Claude Code's exposed contract: `Read` / `Edit` / `Write` are now the canonical tool names, legacy `FileRead` / `FileEdit` / `FileWrite` remain aliases, and file tools expose `file_path` while still accepting legacy `path` / `start_line` inputs. Updated REPL hiding/dispatch, tool flags, plan-file authorization, prompts, and focused coverage. | `cd src/kernel && uv run pytest ../../tests/kernel/tools ../../tests/kernel/tool_authz ../../tests/kernel/orchestrator/test_session_guidance_alignment.py ../../tests/kernel/protocol/test_event_mapper.py::TestToolCallLifecycle ../../tests/kernel/protocol/test_event_mapper.py::TestSubAgent ../../tests/kernel/tools/test_send_message.py ../../tests/kernel/orchestrator/test_tool_executor_parallel.py -q` -> `749 passed, 9 skipped, 24 deselected`; targeted `ruff`; targeted `mypy`; `git diff --check`. Targeted Phase 2 Probe E2E was attempted but blocked before tool execution by an existing path mismatch: Kernel writes auth token under `.deepcli/state/auth_token`, fixture expects `.mustang/state/auth_token`. |
| 2026-05-07 | WebFetch backend management plan | Added a review draft for replacing the bare Playwright WebFetch backend with Crawl4AI, keeping Readability, using FlagManager for feature gates, exposing persistent CLI backend management, and making OOBE an entry point into the same management surface. | `git diff --check -- docs/plans/webfetch-backend-management-plan.md docs/plans/progress.md` |
| 2026-05-06 | Skill activation session-title hardening | Auto-generated session titles now strip internal system-reminder wrappers, summarise skill activation prompts as `/skill args`, and the CLI sanitizes Recent sessions titles to one line before Welcome rendering so old dirty titles cannot break the TUI frame. | `cd src/kernel && uv run pytest ../../tests/kernel/session/test_session_manager.py -q` -> `41 passed`; `uv run pytest tests/e2e/test_skill_activation_e2e.py -q -m e2e` -> `3 passed`; `cd src/kernel && uv run ruff format kernel/session/turns/runner.py ../../tests/kernel/session/test_session_manager.py --check && uv run ruff check kernel/session/turns/runner.py ../../tests/kernel/session/test_session_manager.py` -> `All checks passed`; `cd src/kernel && uv run mypy kernel/session/turns/runner.py` -> `Success`; `cd src/cli && bun run tests/test_session_list_mapper.ts && bun run tests/test_ui_golden_r5.ts`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `git diff --check` |
| 2026-05-06 | Skill slash-command path | Added `_mustang.agent/commands/list` and `_mustang.agent/session/activate_skill`, projected `user-invocable` Skills into CommandManager with `source="skill"`, routed both methods through Hub to the Primary Runtime, and changed CLI interactive/`--print` slash handling so `/skill args` activates Kernel skills instead of reading local files. | `uv run pytest tests/kernel/commands/test_command_manager.py -q` -> `14 passed`; `uv run pytest tests/kernel/session/test_session_manager.py -q` -> `40 passed`; `uv run pytest tests/kernel/protocol/test_routing.py tests/kernel/protocol/test_session_handler.py tests/kernel/agent_runtime tests/kernel/agent_hub -q` -> `85 passed`; `uv run pytest tests/e2e/test_skill_listing_e2e.py tests/e2e/test_skill_activation_e2e.py -q -m e2e` -> `5 passed`; `uv run mypy src/kernel/kernel/protocol/acp/session_handler.py src/kernel/kernel/session/api/handlers.py src/kernel/kernel/agent_runtime/session_service.py src/kernel/kernel/agent_runtime/__main__.py src/kernel/kernel/agent_hub/server.py` -> `Success`; targeted `ruff`; `cd src/cli && bun build src/main.ts --target=bun --outdir /tmp/deepcli-cli-build-skill-command`; `KERNEL_PORT=18328 scripts/run-cli.sh --port 18328 --print "/codex-skill-command-smoke"` -> `SKILL_COMMAND_OK`; `git diff --check` |
| 2026-05-06 | Completed-turn replay de-duplication | Fixed duplicate `clientTurnId` completed-turn replay so it shares the same explicit UI-event vs conversation fallback de-duplication as `session/load`. This prevents router replay from emitting assistant text once from `ConversationMessageEvent` and again from `AgentMessageEvent`, while still recovering conversation-only historical logs. | `cd src/kernel && uv run pytest ../../tests/kernel/session/test_session_manager.py -q` -> `40 passed`; `cd src/kernel && uv run pytest ../../tests/e2e/test_probe_phase2_e2e.py -q -m e2e` -> `34 passed`; `cd src/kernel && uv run pytest ../../tests/e2e/test_skill_listing_e2e.py ../../tests/e2e/test_skill_activation_e2e.py -q -m e2e` -> `5 passed`; `KERNEL_PORT=18306 scripts/run-cli.sh --port 18306 --print "Reply exactly: CLI_OK"` -> `CLI_OK`; targeted `ruff`; targeted `mypy` |
| 2026-05-06 | Router model runtime sync | Routed `_mustang.agent/model/*` methods through Access -> Hub -> Primary Runtime in router mode, corrected ACP model dispatch result schemas, and subscribed active SessionManager instances to `current_used.default` changes so existing sessions using the old default switch before the next prompt. | `cd src/kernel && uv run pytest ../../tests/kernel/session/test_session_manager.py ../../tests/kernel/protocol/test_session_handler.py ../../tests/kernel/protocol/test_routing.py -q` -> `97 passed`; `cd src/kernel && uv run pytest ../../tests/kernel/agent_runtime ../../tests/kernel/agent_hub ../../tests/kernel/llm/test_llm_profile_crud.py -q` -> `53 passed`; `uv run pytest tests/e2e/test_probe_phase2_e2e.py::test_probe_model_set_current_updates_existing_session_runtime -q -m e2e` -> `1 passed`; targeted `ruff`; targeted `py_compile` |
| 2026-05-06 | Skill runtime context bridge fix | Restored Skill system visibility and activation in the real runtime path by forwarding `KernelModuleTable` through `OrchestratorDeps` into `AuthorizeContext` and `ToolContext`, and by injecting the available-skill listing as an explicit system-reminder section. Added regression coverage for the context bridge. | `cd src/kernel && uv run pytest ../../tests/kernel/orchestrator/test_tool_executor_parallel.py ../../tests/kernel/session/test_orchestration_factory.py ../../tests/kernel/skills/test_skill_tool.py ../../tests/kernel/skills/test_skill_manager.py ../../tests/kernel/orchestrator/test_first_principles_prompt.py ../../tests/kernel/session/test_session_manager.py -q` -> `82 passed`; targeted `ruff`; targeted source `mypy`; live Supervisor on `18277` + Probe listed 14 skills including `systematic-debugging`; live Probe `Skill(systematic-debugging)` -> `Activated.`; `git diff --check` |
| 2026-05-06 | LLM stream disconnect retry | Marked OpenAI-compatible HTTP/SSE transport failures, timeouts, and missing `[DONE]` as `transient_transport`; Orchestrator now retries those failures up to 2 times before any streamed output is shown or committed; exhausted/unsafe failures surface as visible assistant error text with `stopReason="error"` instead of being mapped to `end_turn`. | `cd src/kernel && uv run pytest ../../tests/kernel/llm_provider/test_openai_compatible_provider.py ../../tests/kernel/orchestrator/test_orchestrator.py ../../tests/kernel/session/test_client_stream_event_mapper.py ../../tests/kernel/session/test_runtime_helpers.py ../../tests/kernel/protocol/test_routing.py ../../tests/kernel/protocol/test_session_handler.py ../../tests/kernel/session/test_session_manager.py -q` -> `168 passed`; `uv run pytest tests/e2e/test_probe_phase2_e2e.py::test_probe_router_prompt_retries_transient_llm_disconnect -q -m e2e` -> `1 passed`; targeted `ruff check`; targeted `ruff format --check`; targeted source `mypy`; `git diff --check` |
| 2026-05-06 | Split default prompt cleanup | Removed the deprecated shipped single-file orchestrator prompt, kept the default prompt fully split into explicit sections, and added `orchestrator/first_principles` after `# System` and before `# Doing tasks`. Updated prompt override coverage and docs to use real loaded sections. | `cd src/kernel && uv run pytest ../../tests/kernel/orchestrator/test_first_principles_prompt.py ../../tests/kernel/prompts/test_prompt_manager.py -q` -> `23 passed`; `uv run pytest tests/e2e/test_prompt_user_override_e2e.py -q -m e2e` -> `2 passed`; `cd src/kernel && uv run ruff check kernel/orchestrator/prompt_builder.py kernel/prompts/manager.py ../../tests/kernel/orchestrator/test_first_principles_prompt.py ../../tests/kernel/prompts/test_prompt_manager.py ../../tests/e2e/test_prompt_user_override_e2e.py`; `cd src/kernel && uv run mypy kernel/orchestrator/prompt_builder.py kernel/prompts/manager.py ../../tests/kernel/orchestrator/test_first_principles_prompt.py ../../tests/kernel/prompts/test_prompt_manager.py ../../tests/e2e/test_prompt_user_override_e2e.py`; targeted `git diff --check` |
| 2026-05-06 | CLI `/model add` | Added `_mustang.agent/model/add` and wired `/model add` to a two-path TUI flow: existing provider first, or new provider with Kernel-owned provider type options. Existing-provider add shows provider settings read-only and edits only Model Settings; new-provider add reuses the full Provider Settings / Model Settings editor. Saves return to `/model list`. | `cd src/kernel && uv run pytest ../../tests/kernel/llm/test_llm_profile_crud.py ../../tests/kernel/protocol/test_routing.py -q`; targeted `ruff`; targeted `mypy`; `cd src/cli && bun run tests/test_autocomplete_sort.ts && bun run tests/test_editor_slash_argument_autocomplete.ts && bun run tests/test_model_config_editor.ts && bun run tests/test_model_service.ts && bun x tsc -p tsconfig.json --noEmit`; `bun run src/cli/tests/probe_phase_b_pty.ts`; `uv run pytest tests/e2e/test_kernel_e2e.py::test_model_add tests/e2e/test_kernel_e2e.py::test_model_provider_list tests/e2e/test_kernel_e2e.py::test_model_update -q -m e2e`; `git diff --check` |
| 2026-05-05 | CLI model provider type selection | Changed the model config editor's `Type` field from free-text entry to an in-form single-choice provider type selector. Options are derived from Kernel `model/provider_list` provider metadata, and switching type updates the visible provider setting fields from Kernel-owned `settingFields`. | `cd src/cli && bun run tests/test_model_config_editor.ts`; `cd src/cli && bun run tests/test_model_service.ts`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; targeted `git diff --check` |
| 2026-05-05 | CLI `/cost` usage panel | Added `_mustang.agent/session/get_usage` across ACP contracts, routing, Agent Hub/Runtime forwarding, SessionManager usage summarization, and CLI `/cost` rendering. The panel now shows session identity, model/kernel version, context-window occupancy, section breakdown, history, token totals, memory/environment summaries, and an explicit note that dollar pricing waits for trusted pricing tables. | `uv run pytest tests/kernel/commands/test_command_manager.py tests/kernel/protocol/test_routing.py tests/kernel/session/test_session_manager.py -q` -> `82 passed`; `cd src/cli && bun run tests/test_agent_session_adapter.ts && bun run tests/test_input_controller_r4.ts`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; targeted `ruff`; targeted `mypy`; `cd src/cli && bun run tests/test_status_line.ts && bun run tests/run_phase_b.ts`; `uv run pytest tests/e2e/test_cost_e2e.py -q -m e2e` -> `1 passed` |
| 2026-05-05 | Uvicorn uvloop runtime selection | Added a shared uvicorn loop selector and wired Kernel + Access Agent entrypoints to use explicit `uvloop` on non-Windows in both dev and release modes, with a Windows fallback to `asyncio`. Logging behavior was left unchanged. | `uv run pytest tests/kernel/test_entrypoints.py -q`; `cd src/kernel && uv run ruff check kernel/__main__.py kernel/access_agent/__main__.py kernel/uvicorn_runtime.py ../../tests/kernel/test_entrypoints.py`; `cd src/kernel && uv run mypy kernel/__main__.py kernel/access_agent/__main__.py kernel/uvicorn_runtime.py`; uvicorn `Config(..., loop="uvloop").get_loop_factory()` -> `uvloop.Loop`; live release `python -m kernel --port 18250` + `GET /` -> `{"name":"mustang-kernel","version":"1.0.0",...}`; live dev `python -m kernel --port 18252 --dev` + `GET /` -> `{"name":"mustang-kernel","version":"1.0.0",...}` |
| 2026-05-05 | CLI Thinking default collapse | Changed CLI Thinking blocks to default to the collapsed `Thinking...` placeholder while keeping `Ctrl+T` as the reveal/hide toggle. Updated the golden and PTY coverage so interleaved Thinking/tool output starts collapsed and can be revealed on demand. | `cd src/cli && bun run tests/test_input_controller_r4.ts`; `cd src/cli && bun run tests/test_ui_golden_r5.ts`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `cd src/cli && bun run tests/run_phase_b.ts`; root `bun run src/cli/tests/probe_phase_b_pty.ts`; targeted `git diff --check` |
| 2026-05-05 | CLI status-line usage display removal | Removed token/cost/cache/rate usage segments from the status-line presets and segment registry, so the top status bar no longer shows the coin/token total. Kept response duration available as a status-line segment and left session usage accumulation intact for the future `/cost` command. | `cd src/cli && bun run tests/test_status_line.ts`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `cd src/cli && bun run tests/test_ui_golden_r5.ts`; `cd src/cli && bun run tests/run_phase_b.ts`; targeted `git diff --check` |
| 2026-05-05 | WebFetch fetch hardening | Integrated OpenClaw/Hermes WebFetch improvements into the Kernel fetch path: browser-grade headers with `text/markdown` preference, HTTP error results, JSON pretty-printing, HTML sniffing without content-type, binary rejection, success caching, Firecrawl endpoint/maxAge/proxy/storeInCache support, failed-payload normalization, and WebFetch metadata for `title` / `truncated` / `raw_length` / `cached`. | `cd src/kernel && uv run pytest ../../tests/kernel/tools/web/test_fetch_fallback.py ../../tests/kernel/tools/web/test_external_backends.py ../../tests/kernel/tools/web/test_html_convert.py ../../tests/kernel/tools/web/test_web_fetch_tool.py ../../tests/kernel/tools/builtin/test_web_fetch_secondary.py -q` -> `67 passed`; `cd src/kernel && uv run pytest ../../tests/kernel/tools/web/e2e/test_fetch_e2e.py -q -m e2e` -> `6 passed, 4 skipped`; targeted `ruff`; targeted `mypy`; live `WebFetchTool` probe against `https://example.com` -> `backend=httpx`, `status=200`, `error=None`, `truncated=True`, `raw_length=528`, `cached=False`, `contains_example=True`; targeted `git diff --check` |
| 2026-05-05 | CLI response duration display | Added response duration to the assistant message usage footer from ACP `usage_update.durationMs`, so the final `input / output` line now includes elapsed time. Also added a `turn_duration` status-line segment with a streaming elapsed fallback, and exposed live token totals to status-line usage stats without mutating persisted session summaries. | `cd src/cli && bun run tests/test_agent_session_adapter.ts`; `cd src/cli && bun run tests/test_status_line.ts`; `cd src/cli && bun run tests/test_ui_golden_r5.ts`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `cd src/cli && bun run tests/run_phase_b.ts`; targeted `git diff --check`. `bun run src/cli/tests/probe_phase_b_pty.ts` was attempted twice and hit the existing Ctrl+O viewport assertion despite the transcript containing the expected expanded lines. |
| 2026-05-05 | CLI model editor | `/model list` now opens the model selector as a management entry point: selecting a model opens a Provider Settings / Model Settings editor. Added `_mustang.agent/model/update` to persist provider settings, provider/model id renames, model display names, context-window overrides, and exact current-used role assignments; `model/provider_list` now returns provider metadata, kernel-owned `settingFields`, effective endpoints, user-facing provider secrets, and optional `displayNames`. | `cd src/kernel && uv run pytest ../../tests/kernel/llm/test_llm_profile_crud.py ../../tests/kernel/protocol/test_routing.py -q`; targeted `ruff`; targeted `mypy`; `cd src/cli && bun run tests/test_model_service.ts && bun x tsc -p tsconfig.json --noEmit`; `bun run src/cli/tests/probe_phase_b_pty.ts`; `uv run pytest tests/e2e/test_kernel_e2e.py::test_model_provider_list tests/e2e/test_kernel_e2e.py::test_model_update -q -m e2e`; `git diff --check` |
| 2026-05-04 | AgentTool practical stats | Added the practical AgentTool stats path: foreground sub-agent runs now count child tool uses, child turn tokens, and duration, attach the summary to the final Agent tool result as `mustang.agent/agentStats`, carry it through ACP `tool_call_update` metadata, and render the CLI collapsed Agent card as `Done (N tool uses · X tokens · Ys)`. Child transcript/tool events remain private to the Agent tool. Added a separate future plan for full Claude Code AgentTool progress/transcript parity in `docs/plans/agent-tool-claude-code-parity-plan.md`. | `cd src/kernel && uv run pytest ../../tests/kernel/tools/builtin/test_agent.py ../../tests/kernel/protocol/test_event_mapper.py::TestToolCallLifecycle ../../tests/kernel/protocol/test_event_mapper.py::TestSubAgent -q`; `cd src/cli && bun run tests/test_agent_session_adapter.ts && bun x tsc -p tsconfig.json --noEmit`; `cd src/kernel && uv run ruff check kernel/orchestrator/events/agents.py kernel/orchestrator/events/tools.py kernel/orchestrator/agents.py kernel/orchestrator/tools_exec/pipeline.py kernel/tools/types.py kernel/tools/builtin/agent.py kernel/protocol/acp/event_mapper.py ../../tests/kernel/tools/builtin/test_agent.py ../../tests/kernel/protocol/test_event_mapper.py`; `cd src/kernel && uv run mypy kernel/orchestrator/events/agents.py kernel/orchestrator/events/tools.py kernel/orchestrator/agents.py kernel/orchestrator/tools_exec/pipeline.py kernel/tools/types.py kernel/tools/builtin/agent.py kernel/protocol/acp/event_mapper.py`; `cd src/kernel && uv run pytest ../../tests/kernel/tools/builtin/test_agent.py ../../tests/kernel/protocol/test_event_mapper.py ../../tests/kernel/tools/test_send_message.py ../../tests/kernel/orchestrator/test_orchestrator.py -q`; `cd src/cli && bun run tests/test_acp_client_turn_id.ts && bun run tests/run_phase_b.ts`; `cd src/cli && bun run tests/test_ui_golden_r5.ts`; `uv run pytest tests/e2e/test_agent_tool_e2e.py::test_agent_foreground -q -m e2e`; `git diff --check` |
| 2026-05-04 | AgentTool foreground output isolation | Aligned foreground `AgentTool` with Claude Code main: child sub-agent events are no longer passed through as parent session updates, ACP `_meta` is preserved so the CLI can identify sub-agent boundaries, child thought/message/tool updates are kept private inside the Agent tool, and the CLI renders `Agent` with a collapsed custom tool card instead of dumping the child result as generic tool output. The child final text still returns through the Agent tool result for the parent LLM. Updated stale task/orchestrator docs that still described flat passthrough as intended behavior. | `cd src/kernel && uv run pytest ../../tests/kernel/tools/builtin/test_agent.py ../../tests/kernel/protocol/test_event_mapper.py::TestSubAgent -q`; `cd src/kernel && uv run pytest ../../tests/kernel/tools/builtin/test_agent.py ../../tests/kernel/tools/test_send_message.py ../../tests/kernel/orchestrator/test_orchestrator.py -q`; `cd src/cli && bun run tests/test_agent_session_adapter.ts && bun run tests/test_acp_client_turn_id.ts && bun x tsc -p tsconfig.json --noEmit`; `cd src/cli && bun run tests/run_phase_b.ts`; `cd src/cli && bun run tests/test_ui_golden_r5.ts`; `uv run pytest tests/e2e/test_agent_tool_e2e.py::test_agent_foreground -q -m e2e`; targeted `ruff`; targeted `mypy`; `git diff --check` |
| 2026-05-04 | CLI `/model` current-used management | Replaced the old `model/set_default` ACP extension with generic `_mustang.agent/model/set_current`, carrying a role that defaults to `default`. `model/provider_list` now returns `current_used` role refs plus `defaultContextWindow`, the CLI model service uses provider names rather than provider types, `/model` and `/model list` open an OMP-style searchable model selector with Provider/Type and Context details, `/model current` renders the current role table, and `/model use [role] <provider>/<model>` updates the selected role. | `cd src/kernel && uv run pytest ../../tests/kernel/llm/test_llm_profile_crud.py ../../tests/kernel/protocol/test_routing.py -q`; targeted `ruff`; targeted `mypy`; `cd src/cli && bun run tests/test_model_service.ts && bun run tests/test_autocomplete_sort.ts && bun run tests/test_agent_session_adapter.ts && bun run tests/test_input_controller_r4.ts && bun run tests/run_phase_b.ts`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `uv run pytest tests/e2e/test_kernel_e2e.py::test_model_provider_list -q -m e2e`; `bun run src/cli/tests/probe_phase_b_pty.ts`; `git diff --check` |
| 2026-05-04 | Windows dev scripts moved to PowerShell | Added native PowerShell implementations for `run-cli`, `run-kernel`, and `run-probe`, leaving the `.bat` files as thin compatibility launchers that prefer `pwsh` and fall back to Windows PowerShell. The CLI script now owns readiness probing, hidden background Kernel startup, argument passthrough, and Bun executable discovery; the Kernel script writes dev ACP flags as UTF-8 without BOM. Windows CI now checks both `.ps1` and `.bat` entrypoints. | PowerShell syntax parse for all three `.ps1` files; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\run-kernel.ps1 --help`; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\run-probe.ps1 --help`; `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts\run-cli.ps1 --help`; `scripts\run-kernel.bat --help`; `scripts\run-probe.bat --help`; `scripts\run-cli.bat --help`; `cd src/cli && bun run tests/test_startup_args.ts && bun run tests/test_autostart.ts && bun x tsc -p tsconfig.json --noEmit`; workflow YAML parse; targeted `git diff --check` |
| 2026-05-04 | Windows INIT E2E marker guard | Fixed E2E auto-marking to use separator-agnostic `Path.parts` so Windows backslash paths still receive the `e2e` marker and remain excluded from the default non-E2E setup smoke. Clarified `INIT.md` and setup docs that `uv run pytest -q tests/` is not intended to run live-kernel E2E, and recorded the gotcha in lessons learned. | `uv run pytest tests/e2e/test_kernel_e2e.py --collect-only -q` -> `no tests collected (13 deselected)`; `uv run pytest tests/e2e/test_kernel_e2e.py --collect-only -q -m e2e` -> `13 tests collected`; `uv run ruff check tests/e2e/conftest.py`; `uv run pytest -q tests/` now excludes E2E (`155 deselected`) but still has pre-existing Windows non-E2E failures |
| 2026-05-04 | Windows native CLI path/token compatibility | Updated the TypeScript CLI to use native Windows config/state/data paths (`%APPDATA%/DeepCLI`, `%LOCALAPPDATA%/DeepCLI`) while preserving `~/.mustang` as a compatibility fallback. `DEEPCLI_TOKEN` is now accepted alongside `MUSTANG_TOKEN`, default token lookup checks native state before legacy state, Welcome logo lookup uses the same cross-platform path helpers, and the Windows CI smoke job now covers CLI native path/token tests. | `cd src/cli && bun run tests/test_config_loader.ts && bun run tests/test_connect_token_paths.ts && bun run tests/test_startup_args.ts && bun run tests/test_autostart.ts`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `cd src/cli && bun run tests/run_phase_b.ts && bun run tests/run_phase_c.ts`; `cd src/cli && bun run tests/test_ui_golden_r5.ts && bun run tests/test_theme_config.ts`; workflow YAML parse; targeted `git diff --check` |
| 2026-05-04 | Windows native P1 compatibility hardening | Added native Windows dev entry scripts for CLI and Probe, added a `windows-latest` CI smoke job covering Kernel imports, Memory, platform shell selection, and Windows dev script help paths, and unified background shell spawning so Bash background tasks use the same Bash shell as foreground while Monitor uses PowerShell/cmd on native Windows instead of Python's default shell. | `cd src/kernel && uv run pytest ../../tests/kernel/tools/builtin/test_shell_exec.py ../../tests/kernel/tools/builtin/test_bash_background.py ../../tests/kernel/tools/test_monitor.py ../../tests/kernel/tools/test_platform.py ../../tests/kernel/tools/builtin/test_cmd_and_cron_tools.py -q`; `cd src/kernel && uv run pytest ../../tests/kernel/test_entrypoints.py ../../tests/kernel/test_lifespan.py ../../tests/kernel/memory/test_store.py ../../tests/kernel/tools/builtin/test_shell_exec.py -q`; `cd src/kernel && uv run ruff check kernel/tools/builtin/shell_exec.py kernel/tools/builtin/bash.py kernel/tools/builtin/monitor.py ../../tests/kernel/tools/builtin/test_shell_exec.py ../../tests/kernel/tools/builtin/test_bash_background.py ../../tests/kernel/tools/test_monitor.py`; `cd src/kernel && uv run mypy kernel/tools/builtin/shell_exec.py kernel/tools/builtin/bash.py kernel/tools/builtin/monitor.py`; `uv run python - <<'PY' ... yaml.safe_load(.github/workflows/tests.yml) ... PY`; `cd src/cli && bun run tests/test_startup_args.ts && bun run tests/test_autostart.ts && bun x tsc -p tsconfig.json --noEmit`; `bash -n scripts/run-cli.sh scripts/run-kernel.sh scripts/run-probe.sh src/launcher/bin/deepcli`; `git diff --check -- ...` |
| 2026-05-04 | Windows native Memory import blocker fixed | Removed the POSIX-only `fcntl` import as a Kernel startup blocker by making MemoryStore file locking choose POSIX `fcntl.flock` when available and Windows `msvcrt.locking` on native Windows. Added a regression test that reloads `kernel.memory.store` with `fcntl` unavailable, so optional Memory imports no longer break `kernel.app` on Windows before flags can degrade/skip subsystems. | `uv run pytest tests/kernel/memory/test_store.py -q`; `cd src/kernel && uv run pytest ../../tests/kernel/test_entrypoints.py ../../tests/kernel/test_lifespan.py ../../tests/kernel/memory -q`; `cd src/kernel && uv run ruff check kernel/memory/store.py ../../tests/kernel/memory/test_store.py`; `cd src/kernel && uv run mypy kernel/memory/store.py` |
| 2026-05-04 | Dev startup scripts moved to root scripts | Moved the source checkout startup entrypoints into the root `scripts/` directory: `run-cli.sh`, `run-kernel.sh`, `run-kernel.bat`, and `run-probe.sh`. Removed the root `scripts/` ignore rule and updated launcher dev-root discovery plus current docs to use the new paths. | `bash -n scripts/run-cli.sh scripts/run-kernel.sh scripts/run-probe.sh src/launcher/bin/deepcli`; targeted path residue scan; targeted `git diff --check` |
| 2026-05-03 | CLI exit resume hint command formatting | Updated the TUI shutdown resume hint to show the actual lowercase executable command on its own visually distinct line: `deepcli --resume <sessionId>`. This keeps product casing separate from command casing and makes the copyable command easier to notice. | `cd src/cli && bun run tests/test_startup_args.ts`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `cd src/cli && bun build src/main.ts --target=bun --compile --outfile /tmp/deepcli-cli-exit-hint-check`; targeted `git diff --check`; `./install-dev.sh` installed the updated CLI artifact |
| 2026-05-03 | Dev run-cli argument passthrough alignment | Fixed `scripts/run-cli.sh` so dev Kernel startup respects CLI startup flags before forwarding them. `--help` now prints CLI usage without starting Kernel, `--port <port>` starts/probes the dev Kernel on that same port before forwarding the flag, and explicit `--kernel <ws-url>` / `KERNEL_URL` skips default autostart. `--resume <id>` works through the existing final argument passthrough. | `bash -n scripts/run-cli.sh`; `scripts/run-cli.sh --help` showed usage with `--resume`; `scripts/run-cli.sh --port 18631 --print ping` -> `pong`; targeted `git diff --check` |
| 2026-05-03 | CLI resume flag parity | Added `--resume <id>` as an alias for `--session <id>` so the TUI exit prompt's `DeepCLI --resume <sessionId>` command is actually supported. Usage now documents the alias and missing-value errors name `--resume`. | `cd src/cli && bun run tests/test_startup_args.ts`; `cd src/cli && bun run tests/test_session_startup.ts`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `cd src/cli && bun build src/main.ts --target=bun --compile --outfile /tmp/deepcli-cli-resume-check`; `./install-dev.sh` installed the updated CLI artifact |
| 2026-05-03 | CLI lazy session creation on interactive startup | Changed default interactive startup so opening the TUI does not immediately create or load a chat session. `session.startup = "new"` now means "prepare a new session lazily": the CLI lists recent sessions for Welcome, but `session/new` is deferred until the first real chat prompt. Explicit `--new`, `--print`, and direct prompt mode still create a session immediately; shell/Python execution without an active chat session fails instead of creating one. | `cd src/cli && bun run tests/test_session_startup.ts`; `cd src/cli && bun run tests/test_agent_session_adapter.ts`; `cd src/cli && bun run tests/test_config_loader.ts`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `cd src/cli && bun build src/main.ts --target=bun --compile --outfile /tmp/deepcli-cli-lazy-session-check` -> bundled 504 modules; `./install-dev.sh` installed the updated CLI artifact |
| 2026-05-03 | Linux launcher uninstall command | Added `deepcli --uninstall` / `deepcli uninstall` for packaged Linux installs. The command stops the packaged runtime, removes the current user launcher symlink, current launcher artifact, current CLI artifact directory, current managed Kernel venv directory, and runtime state markers while preserving config, sessions, logs, and editable UI assets. Source launcher invocations refuse to uninstall. | `bash -n install-dev.sh src/launcher/bin/deepcli src/launcher/packaging/linux/*.sh`; temp HOME smoke installed through `HOME=/tmp/deepcli-uninstall-home DEEPCLI_RELEASE_DIR=/tmp/deepcli-uninstall-release ./install-dev.sh`, then `HOME=/tmp/deepcli-uninstall-home /tmp/deepcli-uninstall-home/.local/bin/deepcli --uninstall` removed `~/.local/bin/deepcli`, `cli/current`, and `kernel/current` while preserving `assets/welcome-logo.txt` |
| 2026-05-03 | Linux installer noninteractive venv rebuild | Made Linux `install.sh` recreate the managed Kernel venv with `uv venv --clear` so installs never prompt before replacing an existing `.venv`. Cleared a stale pre-fix installer process that was holding the launcher lock and verified the root `./install-dev.sh` path runs noninteractively. | `bash -n install-dev.sh src/launcher/bin/deepcli src/launcher/packaging/linux/*.sh`; `./install-dev.sh` -> rebuilt release artifacts, recreated `~/.local/share/deepcli/kernel/1.0.0/.venv` without a `[y/n]` prompt, installed 42 Kernel packages, and completed successfully |
| 2026-05-03 | Linux installer running-Kernel upgrade handling | Made Linux `install.sh` detect an existing packaged DeepCLI Kernel runtime via the launcher runtime state, stop it before replacing CLI/Kernel artifacts, and restart it after install if it was previously running. Fixed launcher process startup so the detached Supervisor does not inherit the launcher lock fd, and limited automatic dev checkout discovery to the source launcher so installed `deepcli` always uses packaged layout unless `DEEPCLI_DEV_ROOT` is explicit. | `bash -n src/launcher/bin/deepcli src/launcher/packaging/linux/*.sh`; `src/launcher/packaging/linux/install-dev.sh`; `~/.local/bin/deepcli kernel start`; running-upgrade smoke `src/launcher/packaging/linux/install-dev.sh` printed `Stopping running DeepCLI Kernel before install...` and `Restarting DeepCLI Kernel with installed version...`; old runtime pid was dead and `~/.local/bin/deepcli status` reported new runtime ready |
| 2026-05-03 | CLI Thinking visibility full redraw fix | Fixed `Ctrl+T` and settings-driven Thinking visibility toggles so they force a full TUI redraw after updating `AssistantMessageComponent` state. This prevents the differential renderer from skipping hidden/visible Thinking changes that are above the current viewport, especially when thinking is split around tool calls. Expanded the real CLI PTY probe with a hide -> interleaved thinking/tool -> show regression. | `bun run src/cli/tests/test_input_controller_r4.ts` -> `PASS`; repo root `bun run src/cli/tests/probe_phase_b_pty.ts` -> `PASS: Phase B real CLI PTY/TUI probe`; `cd src/cli && bun run tests/run_phase_b.ts` -> `11 passed, 0 failed`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit` -> success |
| 2026-05-03 | Linux Welcome logo customization path | Reworked the packaged Welcome logo fix so the bundled logo is only a fallback. The CLI now loads user-replaceable logo text from `DEEPCLI_WELCOME_LOGO_FILE`, config-dir logo files, or the installed data asset; the Linux release now stages `welcome-logo.txt`, `install.sh` installs it under `~/.local/share/deepcli/assets/`, and launcher exports DeepCLI config/data dirs to the CLI. | `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `cd src/cli && bun build src/main.ts --target=bun --compile --outfile /tmp/deepcli-cli-custom-logo-check` -> bundled 503 modules; `src/launcher/packaging/linux/install-dev.sh`; `DEEPCLI_WELCOME_LOGO_FILE=/tmp/deepcli-custom-logo.txt script -q -c 'timeout 5 ~/.local/bin/deepcli' /tmp/deepcli-custom-logo-smoke.log` displayed custom `CUSTOM` / `DEEPCLI` / `LOGO` text |
| 2026-05-03 | Kernel Phase 1 unit-test coverage completion | Completed the remaining Phase 1 unit-test coverage hardening for the owned high-risk gaps. Added/expanded tests for Agent Runtime session-service ACP contract conversion, MCP resource/auth tools, GatewayAdapter/GatewayManager routing and lifecycle edges, Schedule DeliveryRouter retry/cache/target behavior, and Web fetch/search backend registries plus optional/fallback backends. Coverage moved from `81%` to `84%`; remaining misses are assigned to external/platform/background integration surfaces rather than unowned Phase 1 gaps. | Targeted tranche `cd src/kernel && uv run pytest ../../tests/kernel/tools/builtin/test_mcp_resource_tools.py ../../tests/kernel/tools/builtin/test_mcp_auth.py ../../tests/kernel/agent_runtime/test_session_service.py ../../tests/kernel/gateways/test_gateway_adapter.py ../../tests/kernel/tools/web/test_fetch_fallback.py ../../tests/kernel/tools/web/test_search_fallback.py ../../tests/kernel/schedule/test_delivery.py -q` -> `99 passed`; `cd src/kernel && uv run pytest ../../tests/kernel -q` -> `2036 passed, 9 skipped, 24 deselected`; `cd src/kernel && uv run pytest --cov=kernel --cov-report=term-missing ../../tests/kernel -q` -> `TOTAL coverage: 84%`; `cd src/kernel && uv run ruff check kernel ../../tests/kernel` -> all checks passed; `cd src/kernel && uv run mypy kernel` -> success; `cloc src/kernel/kernel --by-percent c` -> Python comments `36.73%` |
| 2026-05-03 | Linux packaged CLI Welcome logo fix | Inlined the Welcome logo in the active-port Welcome component so the compiled single-file CLI no longer depends on a sibling `welcome-logo.txt` file at runtime. Updated the Linux installer artifact copy path to write a temporary file and atomically replace the executable, avoiding `Text file busy` when reinstalling while an older CLI process is still mapped. | `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `cd src/cli && bun build src/main.ts --target=bun --compile --outfile /tmp/deepcli-cli-logo-check` -> bundled 503 modules; `src/launcher/packaging/linux/install-dev.sh`; PTY smoke `script -q -c 'timeout 5 ~/.local/bin/deepcli' /tmp/deepcli-logo-smoke.log` showed `DeepCLI v1.0.0`, `Welcome back!`, and logo glyphs `⣿`/`⣶` with no module error |
| 2026-05-03 | Linux packaged CLI interactive bundle fix | Fixed the compiled Linux CLI artifact so the active-port TUI interactive mode is bundled into the single executable. Replaced the runtime-only dynamic import with a small JS loader that Bun can statically bundle while keeping the excluded active-port tree out of the main TypeScript typecheck. | `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `cd src/cli && bun build src/main.ts --target=bun --compile --outfile /tmp/deepcli-cli-compile-check2` -> bundled 503 modules; `src/launcher/packaging/linux/install-dev.sh`; `timeout 12 ~/.local/bin/deepcli --print ping` -> `pong`; PTY smoke `script -q -c 'timeout 5 ~/.local/bin/deepcli' /tmp/deepcli-interactive-smoke.log` rendered `DeepCLI v1.0.0` / `Welcome back!` with no `Cannot find module` error |
| 2026-05-03 | Linux installed launcher connection fix | Fixed the packaged Linux launcher to pass the CLI the WebSocket base URL (`ws://host:port`) instead of the full `/session` path, preventing the CLI from connecting to `/session/session`. Packaged Kernel startup now prepends the managed venv `bin` directory to `PATH` so subprocess config entries that invoke `python` resolve inside the bundled runtime. | `bash -n src/launcher/bin/deepcli src/launcher/packaging/linux/*.sh`; `src/launcher/packaging/linux/install-dev.sh`; `~/.local/bin/deepcli kernel start && ~/.local/bin/deepcli status` -> runtime ready; `timeout 12 ~/.local/bin/deepcli --print ping` -> `pong`; `~/.local/bin/deepcli kernel logs` no longer showed `command not found: python` |
| 2026-05-03 | Linux local dev installer | Added `src/launcher/packaging/linux/install-dev.sh`, a one-command developer wrapper that builds release-shaped Linux artifacts from the checkout and installs them through `install.sh` using `DEEPCLI_LOCAL_DIR`, without requiring a published release server. | `bash -n src/launcher/packaging/linux/install-dev.sh`; `HOME=/tmp/deepcli-install-dev-home DEEPCLI_RELEASE_DIR=/tmp/deepcli-install-dev-release src/launcher/packaging/linux/install-dev.sh` -> built launcher + CLI executable + Kernel wheel, installed into temp user layout, and produced `/tmp/deepcli-install-dev-home/.local/bin/deepcli` |
| 2026-05-03 | Linux Bash launcher scaffold | Added `src/launcher/` as the launcher sub-repo boundary with a Bash Linux `deepcli` launcher, Linux packaging scripts, and release manifest example. The launcher supports dev checkout discovery, user-level state/config dirs, `flock` singleton locking, default/fallback port selection, detached Supervisor startup via `setsid`, readiness gating, `status`, `stop`, `restart`, `kernel start`, `kernel logs`, and foreground CLI handoff. | `bash -n src/launcher/bin/deepcli src/launcher/packaging/linux/build-release.sh src/launcher/packaging/linux/install.sh`; `DEEPCLI_DEV_ROOT=$PWD src/launcher/bin/deepcli --help`; `DEEPCLI_DEV_ROOT=$PWD src/launcher/bin/deepcli status`; `DEEPCLI_RELEASE_DIR=/tmp/deepcli-linux-release-check src/launcher/packaging/linux/build-release.sh` -> launcher + CLI executable + Kernel wheel + manifest/checksums; temp state smoke `deepcli kernel start --port 18420` -> ready, `status` -> ready, `stop` -> stopped |
| 2026-05-03 | Linux launcher release planning | Updated the launcher plan with Linux `install.sh` user-level installation, uv-managed Kernel Python runtime, detached Supervisor background semantics without systemd, DeepCLI product state/config paths, WSL2 separation, and CLI artifact handoff. Updated CLI design to make the native launcher the process manager and the TypeScript CLI a bundled foreground ACP client. | `cd src/cli && bun build src/main.ts --target=bun --outdir /tmp/deepcli-cli-build-check`; `cd src/cli && bun build src/main.ts --target=bun --compile --outfile /tmp/deepcli-cli-check`; `/tmp/deepcli-cli-check --help`; `cd src/kernel && uv build --wheel --out-dir /tmp/deepcli-kernel-wheel-check`; `git diff --check -- docs/plans/launcher-subrepo-plan.md docs/cli/design.md docs/plans/progress.md` |
| 2026-05-03 | ACP-only transport stack | Removed the old echo transport stack from production code, made `TransportFlags.stack` default to the only registered stack (`acp`), and rewired route tests to use real ACP `initialize` or test-local fake stacks. Updated active transport docs to describe ACP-only behavior. | `cd src/kernel && uv run pytest ../../tests/kernel/routes ../../tests/kernel/protocol/test_handshake.py ../../tests/kernel/protocol/test_routing.py -q` -> `62 passed`; `cd src/kernel && uv run pytest ../../tests/kernel/test_lifespan.py ../../tests/kernel/test_entrypoints.py -q` -> `12 passed`; `cd src/kernel && uv run pytest ../../tests/kernel/protocol/test_session_handler.py ../../tests/kernel/orchestrator -q` -> `203 passed`; `cd src/kernel && uv run pytest ../../tests/kernel/session/test_orchestration_factory.py ../../tests/kernel/session/test_migrations.py -q` -> `13 passed`; `bash -n scripts/run-cli.sh scripts/run-kernel.sh`; `scripts/run-cli.sh --print ping` -> `pong`; active-code/docs `rg` residue scan; targeted `git diff --check` |
| 2026-05-03 | Dev CLI startup repair | Fixed `scripts/run-kernel.sh` / `.bat` so dev Supervisor starts with an ACP transport flags file by default, preventing CLI `initialize` timeouts against the old non-ACP echo stack. `scripts/run-cli.sh` now ensures the dev Kernel is ready before launching the TUI, Supervisor clears stale child runtime files before waiting, and empty LLM config is a valid no-model state so `session/new` works before providers are configured. | `bash -n scripts/run-kernel.sh scripts/run-cli.sh`; `cd src/kernel && uv run pytest ../../tests/kernel/supervisor/test_supervisor_c1.py -q` -> `19 passed`; `cd src/kernel && uv run pytest ../../tests/kernel/llm/test_llm_manager.py ../../tests/kernel/llm/test_llm_profile_crud.py -q` -> `36 passed`; `cd src/kernel && uv run pytest ../../tests/kernel/session/test_orchestration_factory.py ../../tests/kernel/orchestrator -q` -> `184 passed`; `cd src/kernel && uv run pytest ../../tests/kernel/session -q` -> `126 passed`; from no Kernel process, `scripts/run-cli.sh --print ping` -> `pong`; CLI `test_connect.ts` / `test_session.ts` -> both PASS; `timeout 6 scripts/run-cli.sh` displayed the TUI Welcome; targeted `git diff --check` |
| 2026-05-03 | Kernel/schema version reset | Reset the unreleased Kernel product version from `2.0.0` to `1.0.0` and folded the session DB archive/title-source columns into the pre-release `SCHEMA_VERSION = 1` baseline. Updated D21 with the pre-release reset rule. | `cd src/kernel && uv run pytest ../../tests/kernel/test_entrypoints.py ../../tests/kernel/session/test_migrations.py -q` -> `15 passed`; `cd src/kernel && uv run pytest ../../tests/kernel/session -q` -> `125 passed`; targeted `rg` residue scan; targeted `git diff --check` |
| 2026-05-03 | CLI `Ctrl+O` tool expansion focus hardening | Moved the tool-output expansion shortcut onto the TUI input listener in addition to the editor binding, while letting visible overlays keep their own `Ctrl+O` handling. This keeps `(Ctrl+O for more)` responsive even when focus is not currently on the editor. | `cd src/cli && bun run tests/test_input_controller_r4.ts`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `cd src/cli && bun run tests/run_phase_b.ts` -> `10 passed, 0 failed`; repo root `bun run src/cli/tests/probe_phase_b_pty.ts` -> `PASS: Phase B real CLI PTY/TUI probe`; `git diff --check -- src/cli/src/active-port/coding-agent/modes/controllers/input-controller.ts src/cli/tests/test_input_controller_r4.ts` |
| 2026-05-03 | Reconnect resume startup-race hardening | Fixed the Kernel restart reconnect race where CLI `session/resume` could reach Access before the Primary Runtime re-registered with Hub, producing `runtime_not_registered` as a generic `[-32603] Internal error`. Access now retries transient Hub `runtime_not_registered` responses briefly, and CLI `session/resume` retries transient internal errors before sending the prompt. | `cd src/cli && bun run tests/test_session_resume_retries_transient_internal.ts`; `cd src/cli && bun run tests/test_session_prompt_retries_client_turn.ts`; `cd src/cli && bun run tests/run_phase_c.ts` -> `13 passed, 0 failed`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `cd src/kernel && uv run pytest ../../tests/kernel/protocol/test_session_handler.py -q` -> `22 passed`; `cd src/kernel && uv run pytest ../../tests/kernel/protocol/test_routing.py ../../tests/kernel/agent_hub/test_agent_hub_transport_c.py -q` -> `36 passed` |
| 2026-05-03 | CLI Kernel connection status indicator | Replaced the low-value status-line `pi` glyph with a Kernel WebSocket connection indicator: `⏺` connected, solid rotating circle frames while reconnecting, and `○` disconnected. ACP client now publishes connection state changes and the TUI repaints the status border during reconnect attempts. | `cd src/cli && bun run tests/test_status_line.ts`; `cd src/cli && bun run tests/test_acp_disconnect.ts`; `cd src/cli && bun run tests/test_acp_reconnect.ts`; `cd src/cli && bun run tests/test_ui_golden_r5.ts`; `cd src/cli && bun run tests/run_phase_b.ts` -> `8 passed, 0 failed`; `cd src/cli && bun run tests/run_phase_c.ts` -> `12 passed, 0 failed`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit` |
| 2026-05-03 | CLI terminal title default | Changed the terminal/window title formatter from OMP's `pi: <cwd>` fallback to DeepCLI semantics: default title is `DeepCLI`, and once a session title exists the title becomes that session title. | `cd src/cli && bun run tests/test_terminal_title.ts`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `cd src/cli && bun run tests/run_phase_b.ts` -> `8 passed, 0 failed`; `cd src/cli && bun run tests/run_phase_d.ts`; targeted `git diff --check` |
| 2026-05-03 | CLI `/clear` view-only semantics | Changed DeepCLI `/clear` away from OMP's new-session behavior. It now clears the visible chat/pending/status UI state, keeps the current session intact, and leaves `/session new` as the explicit new-session command. | `cd src/cli && bun run tests/test_input_controller_r4.ts`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `cd src/cli && bun run tests/run_phase_b.ts` -> `7 passed, 0 failed`; `cd src/cli && bun run tests/run_phase_d.ts`; targeted `git diff --check` |
| 2026-05-03 | CLI Welcome display casing | Changed the Welcome title display name from `deepcli` to `DeepCLI` while leaving the lowercase executable name untouched. Updated the UI golden assertion. | `cd src/cli && bun run tests/test_ui_golden_r5.ts`; `cd src/cli && bun run tests/run_phase_b.ts` -> `7 passed, 0 failed`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit` |
| 2026-05-03 | Deferred `/cron` slash command surface | Removed `/cron` from the kernel command catalog, CLI builtin autocomplete, and local slash-command dispatch because Cron exists as ScheduleManager + LLM tools but has no direct CLI command contract yet. Cron tools remain available to the agent. | `bun run src/cli/tests/test_autocomplete_sort.ts`; `bun run src/cli/tests/test_input_controller_r4.ts`; `cd src/kernel && uv run pytest ../../tests/kernel/commands/test_command_manager.py -q`; targeted `git diff --check` |
| 2026-05-03 | CLI `/theme` command completion | Completed the local TUI `/theme current`, `/theme list`, and `/theme set <name>` paths against the ported OMP theme system. `/theme list` now opens the OMP theme selector instead of dumping a long list into the status area; `/theme set` switches the live TUI theme via `setTheme`, refreshes status/editor/UI state, and keeps autocomplete wired to loaded themes. | `cd src/cli && bun run tests/test_input_controller_r4.ts`; `cd src/cli && bun run tests/test_autocomplete_sort.ts`; `cd src/cli && bun run scripts/check_active_port.ts`; `cd src/cli && bun run tests/run_phase_b.ts` -> `7 passed, 0 failed`; `cd src/cli && bun run tests/run_phase_d.ts`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; targeted `git diff --check` |
| 2026-05-03 | CLI slash argument autocomplete parity fix | Removed the DeepCLI-local autocomplete keep/cancel guard that diverged from OMP and closed slash suggestions as soon as a command argument space was typed. `/model ` now transitions from command-name suggestions to argument suggestions, including after Backspace then retyping the space. | `bun run src/cli/tests/test_editor_slash_argument_autocomplete.ts`; `cd src/cli && bun run tests/run_phase_b.ts` -> `7 passed, 0 failed`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; targeted `git diff --check` |
| 2026-05-03 | CLI slash command fallthrough guard | Blocked known-but-unwired autocomplete slash commands such as `/auth` from falling through to the normal LLM prompt path. They now fail locally with a TUI warning until their ACP/local command handlers are wired. | `cd src/cli && bun run tests/test_input_controller_r4.ts`; `cd src/cli && bun run tests/run_phase_b.ts` -> `6 passed, 0 failed`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit` |
| 2026-05-02 | CLI `/session` info compatibility fix | Added the missing `MustangAgentSessionAdapter.getSessionStats()` compatibility surface used by the copied active-port `/session` info command, exposed empty async-job/provider/auth state shims, made provider detail rendering tolerate DeepCLI ACP model objects, and wrapped builtin slash command execution so command errors fail through the TUI error path instead of crashing the process. | `cd src/cli && bun run tests/test_agent_session_adapter.ts`; `cd src/cli && bun run tests/test_input_controller_r4.ts`; `cd src/cli && bun run tests/run_phase_b.ts` -> `6 passed, 0 failed`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; targeted `git diff --check` |
| 2026-05-02 | CLI Welcome kernel version display | Welcome now receives the kernel version from `GET /` via the configured `kernel.health_url` instead of always showing the CLI fallback `0.1.0`. `KERNEL_PORT` / `--port` also update the health URL so non-default ports display the matching kernel version. | `cd src/cli && bun run tests/test_config_loader.ts`; `cd src/cli && bun x tsc -p tsconfig.json --noEmit`; `cd src/cli && bun run tests/run_phase_b.ts` -> `6 passed, 0 failed` |
| 2026-05-02 | DeepSeek OpenAI-compatible provider support | Added a `deepseek` provider type backed by the existing OpenAI Chat Completions transport, defaulting to `https://api.deepseek.com`. DeepSeek thinking is now explicitly toggled off when Kernel thinking is false, and streaming `reasoning_content` maps to `ThoughtChunk` with replay support for DeepSeek tool-call subturns. Updated LLM provider docs and provider type contract references. | `cd src/kernel && uv run pytest ../../tests/kernel/llm_provider/test_format_openai.py ../../tests/kernel/llm_provider/test_openai_compatible_provider.py ../../tests/kernel/llm_provider/test_deepseek_provider.py ../../tests/kernel/llm_provider/test_provider_manager.py -q` -> `33 passed`; `cd src/kernel && uv run pytest ../../tests/kernel/llm ../../tests/kernel/llm_provider ../../tests/kernel/protocol/test_routing.py -q` -> `123 passed`; `cd src/kernel && uv run pytest ../../tests/kernel -q` -> `1978 passed, 9 skipped, 24 deselected` with existing aiosqlite thread warnings; targeted `ruff`; `cd src/kernel && uv run mypy kernel`; changed-file `bandit`; `uv run pytest tests/e2e/test_kernel_e2e.py::test_model_profile_list -q -m e2e` -> `1 passed`; full `uv run pytest tests/e2e -q -m e2e` was interrupted after hanging for ~11 min with no failure output. |
| 2026-05-02 | README messaging refresh | Rewrote the root README marketing focus around DeepSeek V4, AI Coding Agent-built implementation, and the decomposed modular harness with frontend/backend separation. Removed the older long-form tables and internal product-shape exposition from the public entry page. | `git diff --check -- README.md docs/plans/progress.md` |
| 2026-05-02 | Project rename to DeepCLI | Renamed the product/project surface from Mustang to DeepCLI across README, docs, CLI package metadata, Probe package metadata, CLI user-facing strings, ACP handshake title, default orchestrator prompt, OAuth text, web user agents, and related tests. Preserved Mustang as the kernel codename and compatibility namespace for `mustang-kernel`, `~/.mustang`, `MUSTANG_*`, `mustang.agent/*`, and `_mustang.agent/*`. | `uv lock`; `cd src/cli && bun install --lockfile-only`; `cd src/cli && bun run tests/run_phase_b.ts && bun run tests/run_phase_c.ts && bun run tests/run_phase_d.ts && bun build src/main.ts --target=bun --outdir /tmp/deepcli-cli-build`; `cd src/probe && uv run pytest ../../tests/probe/test_client.py -q` -> `16 passed`; `cd src/kernel && uv run pytest ../../tests/kernel/protocol/test_handshake.py ../../tests/kernel/protocol/test_routing.py -q` -> `40 passed`; `git diff --check`; final rename residue scan |
| 2026-05-01 | CLI Phase 3 verification | Ran the current CLI logic, PTY, build, and live Supervisor smoke checks after Probe Phase 2 hardening. The real CLI path passed connect/auth, session/new, prompt streaming, multi-turn context, `--print` English output, and Chinese prompt/output through Supervisor -> Access -> Hub -> Primary Runtime using a local fake OpenAI-compatible model. Found and documented the historical test precondition that sandbox homes had to set `transport.stack: acp`; otherwise the former default echo stack accepted the socket but never replied to ACP `initialize`. | `cd src/cli && bun run tests/run_phase_b.ts` -> `6 passed, 0 failed`; `cd src/cli && bun run tests/run_phase_c.ts` -> `12 passed, 0 failed`; `cd src/cli && bun run tests/run_phase_d.ts` -> all checks passed; `cd src/cli && bun build src/main.ts --target=bun --outdir /tmp/deepcli-cli-build`; repo root `bun run src/cli/tests/probe_phase_b_pty.ts` -> `PASS: Phase B real CLI PTY/TUI probe`; temporary Supervisor on `18267` + fake model -> `tests/run_all.ts` `4 passed, 0 failed`, `src/main.ts --print "Reply with exactly: pong"` -> `pong`, Chinese prompt -> `堪培拉天气查询路径正常。` |
| 2026-05-01 | Probe module-availability coverage expansion | Expanded the deterministic Supervisor -> Access -> Hub -> Primary Runtime Probe suite to 32 scenarios focused on module usability rather than tool internals. Added fixture-backed Probe coverage for Skills, Hooks, Memory, Schedule/Cron, Git context, model/provider extension methods, secrets auth, and GatewayManager webhook missing-adapter behavior. Probe found a real Runtime gap: Primary Runtime did not start trailing `GatewayManager`/`ScheduleManager`, so Cron tools failed in real Runtime turns; Runtime now loads those after SessionManager, matching kernel startup ordering. | `uv run pytest tests/e2e/test_probe_phase2_e2e.py -q -m e2e` -> `32 passed`; `uv run pytest tests/probe/test_client.py tests/e2e/test_probe_phase2_e2e.py -q -m 'e2e or not e2e'` -> `48 passed`; `uv run pytest tests/kernel/agent_runtime/test_session_service.py tests/kernel/protocol/test_session_handler.py tests/kernel/protocol/test_routing.py -q` -> `59 passed`; `cd src/kernel && uv run pytest ../../tests/kernel -q` -> `1974 passed, 9 skipped, 24 deselected`; targeted `ruff`; targeted `mypy` |
| 2026-05-01 | Probe Phase 2 coverage hardening | Expanded the deterministic Supervisor -> Access -> Hub -> Primary Runtime Probe suite from the earlier core 8 scenarios to 21 scenarios. Added router-path coverage for `session/set_mode`, user REPL shell/python execution, cancel execution notification, AskUserQuestion `updated_input`, builtin tools (`Bash`, `Python`, `TodoWrite`, `Glob`, `Grep`, `SendMessage`, `ToolSearch`), and local MCP tool/resource list/read. Fixed a closure-seam bug where router-mode `session/set_mode` and `_mustang.agent/session/execute_shell` / `execute_python` / `cancel_execution` still hit the Access-local SessionManager and failed with `Session not found`; these now route through Hub to Primary Runtime and replay execution updates to Probe. | `uv run pytest tests/e2e/test_probe_phase2_e2e.py -q -m e2e` -> `21 passed`; `uv run pytest tests/probe/test_client.py tests/e2e/test_probe_phase2_e2e.py -q -m 'e2e or not e2e'` -> `37 passed`; `uv run pytest tests/kernel/protocol/test_session_handler.py tests/kernel/protocol/test_routing.py -q` -> `52 passed`; `cd src/kernel && uv run pytest ../../tests/kernel -q` -> `1974 passed, 9 skipped, 24 deselected`; targeted `ruff`; targeted `mypy` |
| 2026-05-01 | Kernel unit-test coverage hardening tranche | Continued Phase 1 beyond the earlier P0 baseline after confirming the suite was not fully covered. Added 69-ish focused tests across legacy agent schema contracts, protocol interfaces, OpenAI-compatible/provider manager, Cmd/Cron tools, external web backends, CronExecutor, MCP health/transport/ws, and process entrypoint helpers. Fixed two unit-test-discovered bugs: process-backed `AgentRuntimeSpec` command validation now actually runs, and Perplexity search handles citations with empty `choices`. Coverage moved from `77%` to `81%`; full coverage is still not complete and remaining gaps are listed in `kernel-unit-test-phase1.md`. | `cd src/kernel && uv run pytest ../../tests/kernel -q` -> `1974 passed, 9 skipped, 24 deselected`; `cd src/kernel && uv run pytest --cov=kernel --cov-report=term-missing ../../tests/kernel -q` -> `TOTAL coverage: 81%`; `cd src/kernel && uv run ruff check kernel ../../tests/kernel`; `cd src/kernel && uv run mypy kernel` |
| 2026-05-01 | Kernel Phase 2 Probe/Supervisor E2E completed | Completed the core Phase 2 Probe validation against a real Supervisor -> Access -> Hub -> Primary Runtime process chain using a local OpenAI-compatible fake model. Added deterministic E2E coverage for readiness/auth, session lifecycle, prompt, duplicate `clientTurnId`, FileRead tool execution, FileWrite permission allow/reject, error propagation, and Probe `--test` JSON output. Fixed two closure-seam bugs found by the Probe: generated Supervisor tokens are now passed as `--flag=value`, and router backend `session/list`/`session/load` now route to Primary Runtime instead of stale Access-local state. | `uv run pytest tests/e2e/test_probe_phase2_e2e.py -q -m e2e` -> `8 passed`; `uv run pytest tests/probe/test_client.py tests/e2e/test_probe_phase2_e2e.py -q -m 'e2e or not e2e'` -> `24 passed`; `cd src/kernel && uv run pytest ../../tests/kernel/agent_runtime/test_session_service.py ../../tests/kernel/protocol/test_session_handler.py ../../tests/kernel/agent_hub/test_agent_hub_transport_c.py ../../tests/kernel/supervisor/test_supervisor_c1.py -q` -> `51 passed`; `cd src/kernel && uv run pytest ../../tests/kernel -q` -> `1905 passed, 9 skipped, 24 deselected`; targeted `ruff`; `cd src/kernel && uv run mypy kernel` |
| 2026-05-01 | Kernel Phase 1 unit-test plan completed | Completed the Phase 1 unit-test pass for Kernel P0 gaps, adding focused tests for runtime sender, client-stream mapping/replay, gateway mixin, orchestration factory, runtime helpers, and Supervisor runtime. Cleaned up ruff/mypy issues surfaced by the final gate. Total Kernel coverage is now `77%`; remaining external/backend warnings are documented for Phase 2/fixture cleanup. | `cd src/kernel && uv run pytest ../../tests/kernel -q`; `cd src/kernel && uv run pytest --cov=kernel --cov-report=term-missing ../../tests/kernel`; `cd src/kernel && uv run ruff check kernel ../../tests/kernel`; `cd src/kernel && uv run mypy kernel` |
| 2026-05-01 | Kernel Phase 1 unit-test execution, first P0 tranche | Ran the Kernel coverage audit, fixed a stale ACP config-option assertion, added targeted unit tests for Agent Runtime sender/session-service boundaries and Session client-stream event mapping. Coverage moved from `75%` to `76%`; remaining P0 gaps are documented in `kernel-unit-test-phase1.md`. | `cd src/kernel && uv run pytest ../../tests/kernel/agent_runtime/test_session_service.py ../../tests/kernel/session/test_client_stream_event_mapper.py ../../tests/kernel/protocol/test_event_mapper.py::TestSessionState::test_config_option_changed -q`; `cd src/kernel && uv run pytest --cov=kernel --cov-report=term-missing ../../tests/kernel` |
| 2026-05-01 | Agent Control Plane plan status refresh | Updated the Agent Control Plane plan to mark the landed single-Primary Supervisor/Hub/Access/Primary Runtime architecture as the current product path, clarified remaining work as peer/child/external runtime expansion, and converted the full-system test plan from gap planning to regression coverage. | `git diff --check` |
| 2026-05-01 | Runtime permission tunnel + Platform Adapter router path | Closed the remaining single-Primary gaps: Primary Runtime can proxy `session/request_permission` through Hub to Access/CLI or Platform Adapter reply sinks; Gateway/Discord adapters route platform prompts through Agent Hub instead of directly calling SessionManager when router backend is active; platform messages generate stable `clientTurnId`; completed duplicate prompt retries replay stored assistant updates without creating a second user message. | `cd src/kernel && uv run pytest ../../tests/kernel/session/test_agent_projection.py ../../tests/kernel/session/test_store.py ../../tests/kernel/session/test_session_manager.py ../../tests/kernel/protocol/test_session_handler.py ../../tests/kernel/protocol/test_routing.py ../../tests/kernel/agent_runtime ../../tests/kernel/agent_hub ../../tests/kernel/gateways/test_gateway_adapter.py -q`; targeted `ruff`; targeted `py_compile`; `cd src/cli && bun run tests/run_phase_c.ts && bun run tests/run_phase_b.ts && bun build src/main.ts --target=bun --outdir /tmp/deepcli-cli-build-final`; `cd src/probe && uv run pytest ../../tests/probe/test_client.py -q`; Supervisor on `8371` + Probe duplicate `clientTurnId` E2E showed `user_messages=1`, `turn_completed=1`, second response replayed `pong` |
| 2026-05-01 | Reconnect-safe prompt idempotency implementation | Implemented Batch C2: ACP prompt `_meta["mustang.agent/clientTurnId"]` now passes through CLI/Probe, Access routing, Hub, Primary Runtime, SessionManager, and SessionStore. SessionManager deduplicates active, queued, completed, and incomplete turns without touching Orchestrator; Agent status projection prefers `clientTurnId`; CLI prompt retry reuses the same id after reconnect. | `cd src/kernel && uv run pytest ../../tests/kernel/session/test_agent_projection.py ../../tests/kernel/session/test_store.py ../../tests/kernel/session/test_session_manager.py ../../tests/kernel/protocol/test_session_handler.py ../../tests/kernel/protocol/test_routing.py ../../tests/kernel/agent_runtime ../../tests/kernel/agent_hub -q`; targeted `ruff`; targeted `py_compile`; `cd src/cli && bun run tests/run_phase_c.ts && bun run tests/run_phase_b.ts && bun build src/main.ts --target=bun --outdir /tmp/deepcli-cli-build-c2-final`; `cd src/probe && uv run pytest ../../tests/probe/test_client.py -q`; Supervisor on `8369` + Probe `--client-turn-id` prompt smoke |
| 2026-05-01 | Agent Runtime prompt timeout fix | Fixed the Hub → Primary Runtime `agent.prompt` contract so real LLM turns are not cut off by the internal 5s websocket timeout, restored a broken `SessionHandlerMixin` indentation in the working tree, and preserved Hub error details across the Access boundary. Permission request forwarding from Runtime → CLI remains a follow-up gap; current runtime fails closed and the turn continues. | `cd src/kernel && uv run pytest ../../tests/kernel/agent_hub/test_agent_hub_transport_c.py ../../tests/kernel/agent_runtime/test_runtime_c.py ../../tests/kernel/protocol/test_session_handler.py -q`; targeted `py_compile`; Supervisor on `8361` + Probe `session/new -> session/resume -> session/prompt`; CLI `KERNEL_PORT=8361 bun run src/main.ts --print "Reply with exactly: pong"`; CLI weather prompt completed without `[-32603]` |
| 2026-05-01 | Reconnect-safe prompt idempotency planning | Added Batch C2 to Agent Control Plane plan, defining `clientTurnId`, retry/recovery semantics, SessionManager/SessionStore changes, Router metadata pass-through, client behavior, platform adapter implications, and acceptance gates. | `git diff --check` |
| 2026-05-01 | CLI WebSocket disconnect/reconnect support | Added `KernelDisconnected`, close/error/ready-state detection, pending request rejection, post-disconnect send guards, background reconnect, interactive-mode disconnect/restored messages, and prompt-time `session/resume` rebind. Routed `session/resume` through Agent Hub to Primary Agent Runtime so a reconnected CLI can rebind the durable session before the next prompt. | `cd src/cli && bun run tests/run_phase_c.ts`; `cd src/cli && bun run tests/run_phase_b.ts`; `cd src/cli && bun build src/main.ts --target=bun --outdir /tmp/deepcli-cli-build`; `cd src/kernel && uv run pytest ../../tests/kernel/protocol/test_session_handler.py ../../tests/kernel/agent_runtime ../../tests/kernel/agent_hub -q`; targeted `py_compile`; temporary Supervisor on `8356` + `KERNEL_URL=ws://127.0.0.1:8356 bun run tests/test_connect.ts` + `... test_session.ts`; `git diff --check` |
| 2026-05-01 | Agent Control Plane Probe E2E closure | Added Hub `agent.prompt` forwarding to registered runtime endpoints, Primary runtime structured prompt response support, Access Agent default `--prompt-backend router` mode, and Supervisor prompt-backend wiring. Router backend now uses Primary Agent Runtime's real SessionManager/Orchestrator/LLM path; `compat` remains an explicit fallback. | `cd src/kernel && uv run pytest ../../tests/kernel/supervisor ../../tests/kernel/agent_runtime ../../tests/kernel/agent_hub ../../tests/kernel/agents ../../tests/kernel/session/test_session_manager.py ../../tests/kernel/session/test_agent_projection.py ../../tests/kernel/protocol/test_routing.py ../../tests/kernel/protocol/test_session_handler.py ../../tests/kernel/tools/builtin/test_agent.py ../../tests/kernel/tools/test_send_message.py ../../tests/kernel/routes -q`; targeted `ruff`; `py_compile`; `git diff --check`; Supervisor on `8347` with default router backend readiness + Probe `ping -> pong` through Router runtime path + CLI connect/session |
| 2026-05-01 | Agent Control Plane Batch G/H backend contracts | Added child DeepCLI kernel launch spec generation for nested Supervisor backends, added an External ACP stdio adapter supporting initialize/session setup/prompt/update collection/cancel/close, and verified runtime-initiated `fs/*` client calls fail closed until permission mediation is wired. | `cd src/kernel && uv run pytest ../../tests/kernel/supervisor ../../tests/kernel/agent_runtime ../../tests/kernel/agent_hub ../../tests/kernel/agents ../../tests/kernel/session/test_session_manager.py ../../tests/kernel/session/test_agent_projection.py ../../tests/kernel/protocol/test_routing.py ../../tests/kernel/protocol/test_session_handler.py ../../tests/kernel/tools/builtin/test_agent.py ../../tests/kernel/tools/test_send_message.py -q`; targeted `ruff`; `py_compile`; `git diff --check`; Supervisor on `8343` readiness + Probe `ping -> pong` + CLI connect/session |
| 2026-05-01 | Agent Control Plane Batch D/F/E Router slice | Kept AgentTool private to parent sessions, added session queue/status projection into Manager runtime records, made Router reject targets not present in the durable routing snapshot, and wired `SendMessageTool` `agent:<id>` through the Router bridge while preserving session/task compatibility. Full peer runtime delivery and Platform Adapter process migration remain future work. | `cd src/kernel && uv run pytest ../../tests/kernel/agent_hub ../../tests/kernel/agents ../../tests/kernel/session/test_session_manager.py ../../tests/kernel/session/test_agent_projection.py ../../tests/kernel/protocol/test_routing.py ../../tests/kernel/protocol/test_session_handler.py ../../tests/kernel/tools/builtin/test_agent.py ../../tests/kernel/tools/test_send_message.py -q`; targeted `ruff`; `py_compile`; `git diff --check`; Supervisor on `8342` readiness + Probe `ping -> pong` + CLI connect/session |
| 2026-05-01 | Agent Control Plane Batch C1 | Added Supervisor entrypoint, standalone Hub/Access/Primary Runtime entrypoints, Supervisor-generated registration token, process runtime files, startup ordering, shutdown cleanup, and Access readiness refresh through Hub internal WebSocket. | `cd src/kernel && uv run pytest ../../tests/kernel/supervisor ../../tests/kernel/access_agent ../../tests/kernel/agent_hub ../../tests/kernel/agent_runtime ../../tests/kernel/routes ../../tests/kernel/session/test_session_manager.py ../../tests/kernel/protocol/test_routing.py ../../tests/kernel/protocol/test_session_handler.py ../../tests/kernel/agents -q`; targeted `ruff`; `py_compile`; `git diff --check`; Supervisor on `8332` readiness + runtime file + Probe prompt + CLI connect/session + cleanup |
| 2026-05-01 | Agent Control Plane Batch C | Added Access Agent metadata/readiness routes, seeded compatibility Agent Hub state during kernel startup, added internal Hub websocket readiness/registration with Supervisor-issued token validation, added minimal websocket Agent Runtime contract, and wired `AgentResourceView.check_and_refresh_before_turn()` into SessionManager turn start. | `cd src/kernel && uv run pytest ../../tests/kernel/access_agent ../../tests/kernel/agent_hub ../../tests/kernel/agent_runtime ../../tests/kernel/routes ../../tests/kernel/session/test_session_manager.py ../../tests/kernel/protocol/test_routing.py ../../tests/kernel/protocol/test_session_handler.py ../../tests/kernel/agents -q`; targeted `ruff`; `py_compile`; `git diff --check`; temporary kernel on `8322` Access readiness + Probe prompt + CLI connect/session |
| 2026-04-30 | Agent Control Plane B1/B2/B | Added injectable `AgentContext` and per-agent session store path support, implemented official ACP `session/close` and `session/resume`, and added no-FastAPI Agent Hub Manager/Router/GlobalResourceMonitor skeletons with AgentDefinitions config schema. Existing `/session` path remains compatible. | `cd src/kernel && uv run pytest ../../tests/kernel/session/test_session_manager.py ../../tests/kernel/protocol/test_routing.py ../../tests/kernel/protocol/test_session_handler.py ../../tests/kernel/agent_hub/test_agent_hub_b.py ../../tests/kernel/agents -q`; targeted `ruff`; `py_compile`; `git diff --check`; CLI `test_connect.ts`/`test_session.ts`; Probe prompt; temporary kernel on `8321` close/resume/prompt probe |
| 2026-04-30 | Agent Control Plane Batch B0 | Added target Supervisor/Agent Hub/Access Agent/Agent Runtime skeleton packages, shared AgentDefinition/RuntimeRecord/RouterFrame/auth schemas, Hub frame encode/decode contract, and Probe/CLI baseline command notes. Runtime behavior unchanged. | `cd src/kernel && uv run pytest ../../tests/kernel/agents -q`; `cd src/kernel && uv run ruff check kernel/agents ../../tests/kernel/agents`; `cd src/kernel && uv run python -m py_compile ...`; `git diff --check` |
| 2026-04-29 | Progress doc compaction | Reduced `progress.md` to a short status ledger and moved durable CLI/kernel milestone history into dedicated docs. | markdown link check; `git diff --check` |
| 2026-04-29 | CLI welcome logo restore | Restored the local welcome logo asset, registered it in the active-port manifest, and added a golden-frame glyph assertion. | `bun run tests/test_ui_golden_r5.ts`; `bun run scripts/check_active_port.ts` |
| 2026-04-29 | Agent Control Plane Batch A | Added `kernel.agents` shared runtime/status/queue/operation vocabulary and `AgentRuntimeController`. Runtime wiring remains future work. | `pytest tests/kernel/agents/test_control_plane.py`; targeted `ruff`; `py_compile` |
| 2026-04-29 | Session mode ACP parity fix | ACP session modes now describe and validate all permission modes: `default`, `plan`, `bypass`, `accept_edits`, `auto`, `dont_ask`. | targeted set-mode E2E; session/protocol tests; full kernel+secret E2E |
| 2026-04-29 | ACP legacy alias removal | Removed temporary unprefixed DeepCLI extension aliases; active clients use `_mustang.agent/*`. | protocol/session tests; CLI typecheck/run_all/PTY probe; full kernel+secret E2E |
| 2026-04-29 | Agent Control Plane planning | Reframed ACPX from runtime dependency to DeepCLI-owned ACP-compatible control semantics. | `git diff --check` |
| 2026-04-29 | ACP `_meta` + execution update migration | Moved custom session metadata and user REPL streaming to namespaced DeepCLI surfaces. | targeted protocol/session tests; CLI checks; live-kernel probes |
| 2026-04-29 | ACP namespace compatibility slice | Added namespace constants/audits and moved CLI/Probe active calls to `_mustang.agent/*` while legacy aliases still existed. | protocol/session tests; CLI checks; live-kernel probes |
| 2026-04-29 | ACP/ACPX reference refresh | Refreshed ACP mirror, added ACPX mirror, and wrote the schema alignment plan. | reference file counts; schema extraction; `git diff --check` |

## Completed Capability Snapshot

### Kernel

- Bootstrap/config/auth: FlagManager, SecretManager, ConfigManager,
  ConnectionAuthenticator.
- Model stack: LLMProviderManager, LLMManager, provider routing, role-based
  `current_used`.
- Protocol/transport/session: ACP stack, WebSocket `/session`, SQLite session
  store, lifecycle actions, mode/config option handling, streaming updates.
- Agent loop: StandardOrchestrator, prompt builder, compaction, plan mode,
  cancellation handling.
- Tools/authz: ToolManager, ToolAuthorizer, BashClassifier, permission
  round-trip, deferred ToolSearch, AskUserQuestion, WebFetch/WebSearch,
  FileRead media support, Todo/Agent/SendMessage tools.
- Extensibility: CommandManager, MCPManager, SkillManager, HookManager,
  MemoryManager, GatewayManager, ScheduleManager, GitManager.
- Control-plane groundwork: `kernel.agents` vocabulary and
  `AgentRuntimeController` protocol.

See [`../kernel/history.md`](../kernel/history.md) for the milestone map and
[`../kernel/subsystems/`](../kernel/subsystems/) for current behavior.

### CLI

- Thin TypeScript/Bun ACP client over WebSocket; no kernel Python imports.
- OMP active-port TUI path with welcome/status/editor, markdown/thinking,
  tool rendering, permission dialogs, session selector, autocomplete, and PTY
  probes.
- ACP-backed session/model/theme/config startup and `/session` / `/model` /
  `/theme` command paths.
- `!` / `!!` shell and `$` / `$$` Python execution routed through kernel ACP
  execution methods.
- Active-port manifest and parity checks guard copied OMP files and local
  assets.

See [`../cli/history/implemented-summary.md`](../cli/history/implemented-summary.md)
for the CLI milestone map.

## Update Rules

- Add only the newest completion to **Recent Log**; remove/merge older rows
  when the table stops being scannable.
- Put implementation details in the owning document:
  `docs/kernel/subsystems/*`, `docs/kernel/history.md`, `docs/cli/*`, or the
  active plan.
- After implementation, include the verification commands that actually ran.
- Gotchas and environment quirks go to [`../lessons-learned.md`](../lessons-learned.md).
